# (no attribute rules currently in force)
#
# agents/ was previously git-crypt encrypted at rest. That was removed
# on 2026-07-28: the folder holds operating procedure, not secrets, and
# one of its files (dark-factory.md) is explicitly written as a public
# teaching artifact. See the root CLAUDE.md for the reasoning.
