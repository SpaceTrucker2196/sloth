# Factory metrics

One row per production order run through the converge loop
(`.claude/commands/converge.md`). Token cost lives in `LEDGER.md`
(script-generated, append-only) — join on the commit sha. Append-only;
never rewrite rows.

| issue | commit | date | converge_iters | tests_at_ship | shipped | notes |
|------:|--------|------|---------------:|--------------:|:-------:|-------|
| 34 | 8714467 | 2026-07-05 | 1 | 3572 | yes | factory standup; docs/tooling only, suite green on first converge |
| 35 | 7d21d42 | 2026-07-14 | 1 | 3672 | yes | allow-list rode #17 SLL2 machinery; suite green on first converge |
| 37 | 08bc4a7 | 2026-07-14 | 1 | 3689 | yes | display-only marker over #35 predicates; prefix election extracted for direct test |
| 38 | 3cdb7cf | 2026-07-14 | 1 | 3710 | yes | add-a-view recipe over existing #31 data layer; green on first converge |
| 39 | 2ca5f4b | 2026-07-14 | 1 | 3778 | yes | docs-drift; extracted view_labels to shared file + capture-based drift guard |
| 40 | 2dbf143 | 2026-07-14 | 1 | 3796 | yes | ICMP-tunnel detector; additive payload_len field + ring-scan rule, green on first converge |
| 45 | a688b25 | 2026-07-20 | 2 | 3813 | yes | factory-infra: risk gate scorer, advisory until owner enables agents/risk_enforce; selftest red once (md-under-agents precedence), green on iter 2 |
| 46 | 4b1b577 | 2026-07-28 | 1 | 3831 | yes | field-reported silent scoping loss; extracted capture_activate_failed as test seam, negative-control run confirmed 4 assertions red on pre-fix logic |
| 47 | f5c848d | 2026-07-28 | 1 | 3842 | yes | #42 follow-up; reset-on-accept via existing fake-accept seam, both mutations (no reset / unconditional reset) confirmed red; issue's "invisible forever" corrected to 300 s heartbeat window |
| 48 | a6af13d | 2026-07-28 | 1 | 4440 | yes | build-config break; maintainer shape call = real ANSI backend + shared palette (src/tui_palette.c), not guard-out or stub-out. CI guard split to e2effaa (+40 surface); combined tree scored RISK 70/50 advisory — see issue for the breakdown |
| 51 | 52cf47a | 2026-07-28 | 1 | 4462 | yes | found by scoring docs/personas/wifi-surveyor.md S2.1 (only WRONG verdict); 802.11k neighbor predicate wired into both alert and view; rescored WRONG->PARTIAL, residual budget-extender case documented not closed |
| 52 | fc1d9d3 | 2026-07-28 | 1 | 4584 | yes | persona-driven (S4.1/S4.2); first operator-supplied-context input — new src/ownership.c module kept out of sloth_state_t so an assertion never sits in the observation record; MY_NET_RECON + flood severity scoping + twin-side pinning; Q4 closed |
| 42 (1/n) | 61700b5 | 2026-07-28 | 1 | 4662 | yes | first slice of the SQLite sink: schema v1 + state bucket. Test caught a real inversion — ASSOC_SRC_* is strongest-first so MAX() preserved the weakest evidence; now a non-zero-guarded MIN. CI dep split to 247731c (+40); combined tree scored RISK 90/50 advisory. Event tables, retention, protocol-flow states still to come |
| 42 (1b/n) | 01dc317 | 2026-07-28 | 1 | 4689 | yes | completes the state bucket (28 tables). Counters upsert MAX so ring eviction can't erase brute-force evidence; SMB dialect sticky to SMB1, RDP proto_mask ORs. SNMP community strings deliberately NOT persisted (secret, unlike the username-class identifiers) — guardrail test scans raw file bytes with a positive control |
| 42 (2/n) | ec4a9b7 | 2026-07-28 | 1 | 4743 | yes | event bucket (36 tables), episode-keyed so a persisting alert is 1 row not 3600. Rewrote the MISSION §2 guardrails: substring checks were too coarse (has_pmkid/pw_observed are legitimate flags) — now type-aware via pragma_table_info plus a raw-file byte scan for seeded key material with a positive control |
| 42 (3/n) | 78305dc | 2026-07-28 | 1 | 4783 | yes | tiered retention (1x/3x/12x) + size ceiling that only ever takes observation rows. Ceiling test caught its own inadequate fixture — 4000 small rows stayed under the 1MiB minimum and passed without exercising the guard; now asserts the precondition |
| 42 (4/n) | 376ab88 | 2026-07-28 | 1 | 4809 | yes | closes the #30/#31 evidence hole — karma_ap_t and rogue_radius_ap_t had no jsonl emitter at all, so detector evidence died on exit. Placed in the finding tier, not entity: at 3x it would expire while the CRIT it justifies was still retained, recreating the hole via retention |
| 42 (5/5) | bf10952 | 2026-07-28 | 1 | 4809 | yes | docs/wiki/sqlite-schema.md — the non-obvious parts (assocs strongest-first ordering, evidence in the finding tier, SNMP deviation from the wire format, auto_vacuum caveat). #42 closed after 5 slices |
| 53 | c1f616c | 2026-07-28 | 1 | 4851 | yes | persona G3 — trajectory-shape classifier (pure, hand-built test trajectories), client RSSI ring, Presence column, persisted class. Mutation-checked; found the interior-peak guard is redundant and said so rather than claiming coverage. S3.1 FAIL->PASS |
| 54 | 85ae491 | 2026-07-28 | 1 | 4895 | yes | persona S3.2 — recurring-transit tracker + alert. Coalescing gap is the load-bearing part (a slow drive-by would otherwise read as a circuit); randomisation solved by resolving through the existing seqnum_correlations table rather than a new primitive. All 3 mutations caught. Q3 closed |
| 55 | c4e7467 | 2026-07-28 | 1 | 5460 | yes | persona G4/Q5 — roster extends ownership.c per #52's precedent. Finding is the roster+designation combination; all 4 config permutations tested. Keys on association not probing because per-SSID randomisation is stable while per-probe is not. Both mutations caught |
| 56 | 43a95a6 | 2026-07-28 | 1 | 5489 | yes | persona G5/S5.2 — sessions table + new-since query + report section; the data was already there from #42, only the boundary was missing. Bumped schema to v2 correcting #53 (added a column without a bump, which CREATE TABLE IF NOT EXISTS cannot apply) and moved the version check ahead of schema application |
| 50 | e7ddd7a | 2026-07-28 | 1 | 5499 | yes | --headless + --no-color/NO_COLOR. Measuring the reported symptom found a worse defect: stdin at EOF made select() return instantly so the refresh interval never applied — ~76k redraws/2s burning a core. isatty guard fixes interactive and --no-color runs too |
| B3b | f3682c1 | 2026-07-28 | 1 | 5530 | yes | roadmap item, no issue — unify the two Wi-Fi IE parsers. Extraction behaviour-preserving; 11 tests enter the shared parser the way nl80211 does. Mutation found an untested MFP-required branch. linux_wifi.c is Linux-only so that half is CI-verified only |
| B3 | 8166b6a | 2026-07-28 | 1 | 5636 | yes | retry/FCS tracking. Extracting radiotap from probe.c (not in the test build) gave it coverage for the first time and surfaced a latent bug: the ext-present-bitmap loop never advanced, so those captures yielded no signal or channel at all. 4/4 mutations caught. Also wrote the missing docs/views/channel.md |
| 57 | 989c5d0 | 2026-07-29 | 1 | 5644 | yes | follow-up I filed against my own #46 review: the activate fix closed one route to a no-ifindex datalink, three others stayed silent. Checked the end state (pkt_linktype) rather than the routes, so future ones are covered. WITH_PCAP=0 caught a real gap — capture.c was in TEST_SRCS but not SRCS, so the new call linked in tests and failed in the no-pcap build |
| 58 | 3f1e789 | 2026-07-29 | 1 | 5649 | yes | the one standing -Wformat-truncation warning, a false positive: GCC can't prove buckets[].src is terminated so assumes %s reads on into the rest of buckets[] (1599 B). %.45s bounds it without truncating a real IPv6 addr. Paired with an assertion that full-length v6 addrs survive, verified red at %.20s — a silenced warning with no test is a cosmetic edit |
| 59a | 5432574 | 2026-08-04 | 1 | 5706 | yes | #59 slice 1 of 4 (dispatcher + BTM parse only) — the slice #61 is blocked on, so it ships alone. Spec conflict found: issue calls Disassociation Imminent "bit 3", IEEE Fig 9-924 says B2; implemented per spec with a test pinning both constants. Optional BSS-Term-Duration/Session-URL skip order is the real hazard — wrong length still returns a plausible parse, so each combination is tested and both mutations verified red |
| 60a | 5efb233 | 2026-08-04 | 1 | 5741 | yes | #60 slice 1 of 5 — AKM/pairwise bitmaps in the shared RSN parser, the prerequisite for both this issue's ask-vs-grant delta and #62's transition-mode rule. String AKM was untestable for "PSK and SAE both present": SAE substring-matches FT-SAE, and the 3-entry display cap drops the 4th suite. Zero-on-entry test found a latent bug — beacon_parse_ies zeroed field-by-field, so its documented "fully zeroed" contract broke on any new field; now one memset |
| 60b | 4fbe9f3 | 2026-08-04 | 1 | 5795 | yes | #60 slice 2 of 5 — assoc/reassoc request parse + request table. Reused beacon_parse_ies for the IE walk (B3b) rather than a 4th copy of the loop; discarded its channel/enc outputs as AP-side notions a client frame doesn't carry. Reassoc's 6-byte Current AP Address is the hazard — wrong length shifts every IE but still "parses". Test constant 11:22:.. turned out group-addressed (I/G bit), correctly rejected by the unicast guard |
| 60c | 487b617 | 2026-08-04 | 1 | 5823 | yes | #60 slice 3 of 5 — downgrade delta, but NOT as specified: the issue reads "granted" AKM from the assoc response, which carries no RSNE outside FT/OWE (§9.3.3.7), so that value does not exist for WPA2-PSK or WPA3-SAE. Implemented the observable form instead — a retry asking for less. Both false-positive guards (PSK-arm requirement, strict-< on MFP) mutation-verified; without them an open-BSS roam and every ordinary retry fire |
| 60d | 123a6e8 | 2026-08-04 | 1 | 5852 | yes | #60 slice 4 of 5 — ASSOC_FLOOD, the missing member of the flood set. Dropped the issue's "<= 3 distinct STAs" gate: assoc-flood tooling randomises source MACs, so a real flood is MANY distinct STAs and the gate would suppress the common case while firing on the rare one. Report the count and name the shape instead. Mutation note: the window filter needs both the outer seed check and inner count check removed to fail |
| 60e | a3f5b4d | 2026-08-05 | 1 | 5867 | yes | #60 slice 5a — JSONL wifi_assoc_req + SQLite assoc_reqs + snapshot. DB_SCHEMA_VERSION 2->3 (#59/#61 can ride it). downgrade_flags OR-accumulates: recovery doesn't erase having been moved. Found that reverting the version constant fails NO test — the mechanism is covered but a table added without a bump is undetectable automatically, since fresh files get every table regardless. Added the adjacent check that can be automated: every table needs a retention-tier ageing column |
| 59b | 2cf45d9 | 2026-08-24 | 1 | 6026 | yes | #59 slice 2 of 4 — ALERT_TYPE_BTM_ABUSE. Gated on Disassociation Imminent rather than rate alone: the issue's literal rate rule fires on ordinary 802.11v load balancing, so both counts go in the detail and only the forcing subset crosses the threshold. Two structures per assoc_track precedent — durable pair row for the view, timestamped ring for the window. 5/5 mutants caught, but the candidate-list memset survived round one: overwriting slot 0 hides it until the new list is SHORTER, so the test now shrinks 2 candidates to 1. STA_A in the existing test file is group-addressed (I/G bit) — the same trap #60 slice 2 hit with the same constant |
| 59c | 3aceecf | 2026-08-24 | 1 | 6070 | yes | #59 slice 3 of 4 — SQLite btm_requests + JSONL + [a] view section + --report. No schema bump needed: db_open applies the schema after the version check, so CREATE TABLE IF NOT EXISTS covers a new table on a v3 file — verified by reading db_open rather than trusting slice 5a's note. The view's empty-deauth early return would have hidden the section in exactly the case it exists for (802.11v moves a client with no deauth frame); found by writing the test first. View tests now capture stdout per test_help.c — as smoke tests they'd have passed against a draw rendering nothing. Counters upsert MAX so LRU eviction can't erase evidence behind a live alert. 3/3 mutants caught |
| 59d | 4052fa3 | 2026-08-24 | 1 | 6070 | yes | #59 slice 4 of 4 — docs/wiki/btm-abuse.md + [a] view section + alerts rule row + ROADMAP. Closes #59. The wiki page leads with what a false positive looks like (band steering, a dying radio, a stubborn client) because a behavioural-threshold detector with no such section gets distrusted on its first benign fire. Found two #60 slice-5a drifts while writing: sqlite-schema.md still said v2/40 tables and jsonl-schema.md had no wifi_assoc_req row — fixed rather than documented around, since adding btm_requests beside them would leave the reference more wrong. The schema doc independently confirms slice 3's no-bump call |
| 60f | 10dc6bf | 2026-08-24 | 1 | 6107 | yes | #60 slice 5b of 5 — [w] asked column + delta marker, [k] PHY corroboration, --report, docs. Closes #60. RISK 65/50 advisory, driven by jsonl.c + jsonl-schema.md scoring as an external-contract proxy; the change is one added field, so additive, flagged in the commit rather than waved through. No granted_akm column exists to build: an assoc response carries no RSNE outside FT/OWE (§9.3.3.7). Delta rides as a trailing marker because the row was already 110 cols and prev_akm would read '-' on nearly every row. PHY corroboration enriches existing PNL rows only and leaves probe_count alone — an assoc request is not a probe. Skipped the pnl_clients.phy_confirmed column: a column on an existing table needs v3->v4, which makes every existing database refuse to open, for a display nuance. 3 mutants, 1 survived round one (clean-row check tested for '->' but a flagless marker block still emits a bare delta glyph) |
| 62 | dd736de | 2026-08-25 | 1 | 6151 | yes | #62 — ALERT_TYPE_WPA_DOWNGRADE, four lanes, one alert per (BSSID,kind). RISK 65/50 advisory (jsonl.c + churn; the JSONL change is two added fields). Fired from a rule reading the AP table, not the parser: beacon_parse_ies is pure and reached by both Wi-Fi paths, and the 30s floor needs per-BSSID history. downgrade_flags lives on the record and is filled once per poll so the rule and the [b] view can't drift. MFP-optional narrowed to SAE-only and WPA1 to WPA1-with-RSN — both would otherwise double-count findings wifi_assess already reports. [b] MFP column became posture. 4 mutants, 1 survived round one: the no-pair test had a single AP so removing the OPEN check changed nothing |
| 65 | bf4029e | 2026-08-25 | 1 | 6189 | yes | #65 — PEAP_NO_SERVER_CERT, CVE-2023-52160. RISK 55/50 advisory (new file + churn). Widened eap_track_observe to carry STA and direction — both already derived at the one call site, never passed. Direction is load-bearing: a Certificate travelling STA->AP is the client authenticating and would mask an AP with none. EAP-Failure deliberately not a finding — the client refused, which is correct. No fragment reassembly, so the detail distinguishes 'no ServerHello' (unambiguous) from 'no Certificate observed' (may have crossed a boundary); [z] TLS? column never shows a tick because absence of a finding isn't proof. 4 mutants: one surfaced DEAD CODE rather than a missing test — the entry guard in tls_scan_handshake exactly duplicated the loop's record-header check, removed, real check verified red |
| 63 | fde7bc4 | 2026-08-25 | 1 | 6231 | yes | #63 — ALERT_TYPE_CSA_ABUSE. RISK 65/50 advisory. Storm counts DISTINCT destinations not announcements: an AP repeating one switch per beacon is the standard working, and counting frames would fire on every DFS event. Beacon addr2/addr3 mismatch deliberately not a forgery — equal by construction, so a difference means misparse. [b] PHY cell becomes CSA>N while pending rather than adding a column. 3 mutants: the third surfaced a WRONG COMMENT — the per-BSSID loop guard claimed to prevent per-beacon firing but fire() already dedups by key, so it only saves a ring scan. CSA-and-vanish deferred: needs positive dwell evidence on a hopping sensor |
| 66 | 3510c76 | 2026-08-25 | 1 | 6257 | yes | #66 — HT/VHT/HE/EHT operation IE decode. Durable 6GHz fix: HE Operation 6GHz Info now outranks DS Param, which much 6GHz gear omits; channel_source makes a wrong channel attributable. Three overstate-hazards each pinned: 40MHz needs the width bit AND a secondary offset, VHT width 0 must not overwrite HT, 160 vs 80+80 by centre gap. HE optional-field order (VHT-op, co-host, 6GHz) is the real trap — wrong skip yields a plausible channel, not a failure. CH_WIDTH_UNKNOWN kept distinct from 20: not-decoded is not narrow. Width upgrade-only per record. MCS ceiling deliberately not stored — no reader yet. 4/4 mutants caught |
| 61 | e72ec0f | 2026-08-25 | 1 | 6298 | yes | #61 — RRM_SURVEY_ABUSE. Discriminator is context not rate: fires only when the asker has beaconed SOMETHING but never the SSID asked about. That two-part test is the --hop guard — a BSSID never heard tells us nothing about what it advertises, and collapsing the halves fires on every AP the sweep missed (mutation-verified). Broadcast requests never count toward the threshold; reports counted separately since a reply proves success not attempt. ssid_history exonerates multi-VAP. Footer not column. 3/3 mutants caught |
| 64 | 93d1dd0 | 2026-08-25 | 1 | 6336 | yes | #64 — src/ctrl_frames.c + ALERT_TYPE_RTS_FLOOD. The dispatch had to go ABOVE probe.c 24-byte management guard: every control frame is shorter (CTS 14, RTS 20), so placed below it the module would test green and count nothing in the field. CTS/ACK carry only an RA — attribution comes from the subtype, never the length: a captured CTS includes a 4-byte FCS so a length guess reads 6 bytes past RA and invents a TA from the checksum. First CTS test used bare 10-byte frames, too short for the mutant to bite; now 16-byte realistic ones. Sustain required, not just rate. Illegal NAV (>32767us) is CRIT since no conforming radio emits one. snapshot annotates channel rows, never creates. 3/3 caught after strengthening **Process note**: `git add -A` failed on ignored paths, so the `&&` chain skipped the feature commit while leaving everything staged — the next `git add METRICS.md && git commit` then swept the whole feature into a `chore(metrics)` commit. Already pushed, so not rewritten; rationale recorded in the follow-up `docs:` commit instead. Chain `git add` with explicit paths, not `-A` with exclusions |
| 70 | 2e55128 | 2026-08-25 | 1 | 6371 | yes | #70 — ALERT_TYPE_BLOCKACK_ATTACK (Bl0ck, arXiv 2302.05899). The 12-bit SSN wrap is the whole hazard and was written up on the issue before any code existed: computed as plain subtraction 4090->10 reads as a jump of 4080 rather than an advance of 16, so the rule fires constantly on healthy links — green in CI, useless on air. Modulo 4096, with bar_ssn_forward_distance exposed so the arithmetic is tested directly. Per (receiver, transmitter, TID) — traffic classes have independent sequence spaces. First BAR sets a baseline not a jump, since every connection is joined mid-flight. Multi-TID BARs skipped not misread. Retry burst reported not required — requiring it makes the rule depend on rf_quality sampling the right channel. 4/4 mutants caught |
| 72 | 8d1efd6 | 2026-08-25 | 1 | 6468 | yes | #72 — 802.11 data-to-IP bridge. Arithmetic split into src/dot11_data.c because capture.c is absent from TEST_SRCS and a wrong header length does not fail loudly: it yields a plausible ethertype from payload bytes. The three addends compose (24+6+2+4=36) — treating them as alternatives gets every SINGLE modifier right and the combination wrong, mutation-verified. Encrypted rejected per MISSION §2, as its own status not lumped with malformed, since on most networks it is the normal case. A-MSDU bit read at the computed QoS offset, not a fixed one — it moves when addr4 is present. Truncation test walks every cut point. 4/4 mutants caught. #69 still needs #71 |
| 67 | 5c7d134 | 2026-08-25 | 1 | 6521 | yes | #67 — MLE decode + MLD canonical identity. Not a feature so much as a live mis-count: an MLD radios have independent sequence spaces so seqnum correlation never links them, and one Wi-Fi 7 handset read as three devices. MLE beats seqnum in transit_canonical_mac — assertion over inference; the test makes them disagree and asserts which won. **Found and fixed a regression from #66**: that commit dropped the line setting has_eht while restructuring the tag-255 branch — variable stayed declared and read, compiled clean, suite green, and every AP silently stopped being Wi-Fi 7. Nothing covered the PHY ladder from beacon IEs; now one test per rung. Link Info found via declared Common Info Length not by summing known optionals. Links merge, never replace. 4/4 mutants caught |
| v4 | 03f977d | 2026-08-25 | 1 | 6538 | yes | Batched DB_SCHEMA_VERSION 3->4 — four columns deferred across #60/#62/#65 taken as one break instead of three. All counters upsert MAX/OR, never excluded: bounded LRU tables restart at zero and overwriting erases evidence behind a live alert; downgrade_flags ORs so a reconfigured AP still shows what it once offered. 4/4 mutants caught. **Found a Makefile bug doing it**: TEST_BIN listed no header prerequisites, so a header-only change never rebuilt the test binary — bumping the version and re-running reported on the previous build, making a mutation look like it survived. Every header-only change in this repo history was tested against a stale binary. Fixed; verified both directions |
| 68 | 6c90c75 | 2026-08-25 | 1 | 6582 | yes | #68 — tool-fingerprint mechanism with an EMPTY signature table, by design: a tool fingerprint is an empirical fact about a binary, and invented rows pass every test while matching nothing. PMKID-harvest half works today — a protocol observable, not a vendor quirk; BSSID match load-bearing or every KARMA AP gets someone else evidence. Testing an empty table took two goes: guards are unreachable through the normal entry point, so tests now drive tool_fingerprint_match_table() — the shipped code, not a mirror of its logic. Second mutation found the hits==0 guard provably redundant (strict best-match from zero already rejects it); removed, property re-verified by relaxing the comparison. Also consolidated KARMA_SSID_THRESH, defined twice with the same value |
| 71 | 63988d0 | 2026-08-25 | 1 | 6634 | yes | #71 — HTTP response parsing. The segmentation problem IS the issue and body_complete is the answer: sloth does not reassemble TCP, so the parser reports three states not two, and a consumer must compare bytes only when complete. Different and incomplete are different answers; conflating them turns every large response into a reported hijack. Chunked never complete — the test that earns it is chunked WITH a matching Content-Length, since every other route to incomplete is closed there; that is also the attacker evasion, named in the docs. Pipelining left unpaired, pairing consumes the request, continuation segments rejected. 4 mutants, 1 survived round one: the chunked test reached body_complete=0 by another path so the branch was never under test |
| 69 | d9461d4 | 2026-08-25 | 1 | 6687 | yes | #69 — captive-portal interception. Sentinel table ships FULL (published fixed values) — the opposite of #68 and for the opposite reason. Three independent signals so a portal evading one still fails the others: chunked responses evade the body check and cannot evade DNS. Everything rests on #71 body_complete: a prefix differing from a sentinel is a segment boundary, not an interception, and comparing anyway reports every large page as a hijack. One alert type with kinds, not the seven proposed. DNS_UNEXPECTED is WARN since CDN ranges change. No [C] view — episodic events, [h] decoration instead, flagged. Option 114 and credential-POST named as gaps. 4/4 mutants caught |
| cli | 13b5721 | 2026-08-30 | 1 | 6708 | yes | --version/-V flag + audit of what the stale-test-binary bug could have hidden. Audit clean: across 400 commits only 7 changed a header without touching a tested .c — 6 are SLOTH_VERSION release bumps (version tests already decoupled) and 1 is a29ed7e whose palette constants had no tested consumer then, since tui_palette.c was extracted later. Confirmed the fix rather than assuming: header-only edit now rebuilds, and moving CP_BR_EXTRA_BASE into another bank fails 10 assertions. The new test pins the failure that actually happens — a release forgetting the bump ships a binary claiming to be its predecessor and the updater reports an upgrade forever |
| 73a | 3987e81 | 2026-08-31 | 1 | 6781 | yes | #73 slice 1 of 3 — research corpus + FTS5 ingest. RISK 50/50 advisory (12 new files + churn). Verified both premises rather than trusting: FTS5 compiled in, and three builds byte-identical (sorted path order) so committing research.db is sound. Found the issue proposed [q] for the view — q is the QUIT key, checked before the view switch; free letters are c and f. Parser refuses over half-accepts: a colonless frontmatter line is an error because a typo-d alert_kinds means a doc that never surfaces, and an over-long list is refused not truncated. Guard half-enforced: nonexistent-alert-kind is enforced (whole-token, so ALERT_TYPE_ROGUE cannot pass as a prefix of _ROGUE_RA — both mutation-verified), coverage is warning-only at 11/47 |
| 73b | 0da14f3 | 2026-09-03 | 1 | 7041 | yes | #73 slice 2 (query layer half) — the four ops as a LIBRARY, sloth links it directly rather than spawning a subprocess to query its own file; MCP becomes a transport wrapper. RISK 75/50 advisory. Three bugs the tests found: (1) unicode61 splits on underscores so alert_kinds MATCH ALERT_TYPE_EVIL_TWIN also matches docs naming only _PROXIMITY — now delimiter-wrapped LIKE; (2) for_alert returned sections not documents, so one source appeared once per heading; (3) the report keyed on the display TITLE, which is abbreviated (BLOCKACK_ATK vs ALERT_TYPE_BLOCKACK_ATTACK), silently losing citations — added alert_type_name(). posture_render_md now takes the handle instead of a main.c global: the link failure was the useful kind, since main.c is not in TEST_SRCS and the block was untestable. 5/5 mutants caught |
| 73c | 60cd9eb | 2026-09-03 | 2 | 7329 | yes | #73 slice 2 (MCP transport) — sloth-research-mcp over newline-delimited JSON-RPC, built by `make research-mcp`, not in `all`. RISK 50/50 advisory. Wrote a bounded recursive-descent JSON reader rather than vendoring one or scanning for `"key":` — a scanner reads the tool name out of the user own arguments when they contain `"name"`; there is a test that fails under both a scanner and a prefix matcher. mcp_handle() is a pure fn of (corpus, request, clock) so the wire shapes are testable without spawning a process. Two bugs found: rq_recent was open-ended above (days=1 at any past clock returned the whole corpus — invisible in production until a retrieved date or a clock is wrong, then the weekly brief is silently complete); research-index had been building with -Wcomment since slice 1 because the target is not in `all`. TEST_HDRS extended to research/ — the #67 stale-binary hole was still open there. 11/11 mutants caught, 3 needed new tests (exact key lookup, %g exponent switch above 1e16, and a buffer-size sweep asserting every size yields MCP_OVERFLOW or a complete message) |
| 75a | 366d342 | 2026-09-03 | 1 | 7397 | yes | #75 slice 1 — FragAttacks CVE-2020-26140/-26143 (FRAG_PLAINTEXT) and -26145 (FRAG_BCAST). RISK 50/50 advisory. The gate is the design: per (BSSID, station) and ORDERED, not `the beacon advertised RSN`. Beacon-keyed is unusable — silent on a hopping radio, and association happens in the clear so a per-BSS flag turns every mid-association client into a CRIT against the whole network. Four exemptions each load-bearing (EAPOL/Null/continuation-fragment/non-SNAP). Added dot11_data_addrs(): addr3 is only the BSSID on an IBSS or uplink frame; reading it unconditionally merges every station into one bogus BSS on downlink. NOT building the A-MSDU detector as specified (§4.2) — subframe headers are inside the ciphertext, so the issue premise is unobservable; replacement signal is a duplicate seqnum with a differing A-MSDU bit, slice 3. Corrected the issue CVE mapping (-26146 is non-consecutive PN, broadcast is -26145). Found my own brittle test from 73c: the MCP buffer sweep hard-coded 1200 bytes, so adding a corpus doc broke it — now sweeps to the response natural length. 12/12 mutants caught |
| 75b | c664cb8 | 2026-09-04 | 1 | 7456 | yes | #75 slice 2 — frag_session_t table keyed (BSSID,SA,DA,TID) + FRAG_CACHE (CVE-2020-24586) and FRAG_MIXED (CVE-2020-26147). Neither gated on slice 1's RSN-witnessed state — the defrag buffer exists before decryption. frag_note_association() is its own table, not a reuse of assoc_track.c's (that one merges by evidence strength, wrong rule for "did the cache-clearing event happen"). Dropped -26146 (non-consec PN) despite the wiki page having pre-committed it to this slice: the CCMP PN is one counter shared by every frame on a (key,TID), not per-reassembly, so ordinary interleaved traffic and fragment retries both open gaps that would read as attacks — corrected in docs rather than shipped unusable. Added dot11_data_tid(). research.db regenerated (13/49 -> 15/51 coverage, still warning-only). RISK 15/50 advisory, no schema/JSONL change. 6/6 mutants caught (endpoint OR, strict after-comparison, session close, mixed-protect compare, fragment-zero/continuation branch, session eviction) |
