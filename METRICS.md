# Factory metrics

One row per production order run through the converge loop
(`.claude/commands/converge.md`). Token cost lives in `LEDGER.md`
(script-generated, append-only) — join on the commit sha. Append-only;
never rewrite rows.

| issue | commit | date | converge_iters | tests_at_ship | shipped | notes |
|------:|--------|------|---------------:|--------------:|:-------:|-------|
| 34 | 8714467 | 2026-07-05 | 1 | 3572 | yes | factory standup; docs/tooling only, suite green on first converge |
| 35 | 7d21d42 | 2026-07-14 | 1 | 3672 | yes | allow-list rode #17 SLL2 machinery; suite green on first converge |
| 37 | 08bc4a7 | 2026-07-14 | 1 | 3689 | yes | display-only marker over #35 predicates; prefix election extracted for direct test |
| 38 | 3cdb7cf | 2026-07-14 | 1 | 3710 | yes | add-a-view recipe over existing #31 data layer; green on first converge |
| 39 | 2ca5f4b | 2026-07-14 | 1 | 3778 | yes | docs-drift; extracted view_labels to shared file + capture-based drift guard |
| 40 | 2dbf143 | 2026-07-14 | 1 | 3796 | yes | ICMP-tunnel detector; additive payload_len field + ring-scan rule, green on first converge |
| 45 | a688b25 | 2026-07-20 | 2 | 3813 | yes | factory-infra: risk gate scorer, advisory until owner enables agents/risk_enforce; selftest red once (md-under-agents precedence), green on iter 2 |
| 46 | 4b1b577 | 2026-07-28 | 1 | 3831 | yes | field-reported silent scoping loss; extracted capture_activate_failed as test seam, negative-control run confirmed 4 assertions red on pre-fix logic |
| 47 | f5c848d | 2026-07-28 | 1 | 3842 | yes | #42 follow-up; reset-on-accept via existing fake-accept seam, both mutations (no reset / unconditional reset) confirmed red; issue's "invisible forever" corrected to 300 s heartbeat window |
| 48 | a6af13d | 2026-07-28 | 1 | 4440 | yes | build-config break; maintainer shape call = real ANSI backend + shared palette (src/tui_palette.c), not guard-out or stub-out. CI guard split to e2effaa (+40 surface); combined tree scored RISK 70/50 advisory — see issue for the breakdown |
| 51 | 52cf47a | 2026-07-28 | 1 | 4462 | yes | found by scoring docs/personas/wifi-surveyor.md S2.1 (only WRONG verdict); 802.11k neighbor predicate wired into both alert and view; rescored WRONG->PARTIAL, residual budget-extender case documented not closed |
| 52 | fc1d9d3 | 2026-07-28 | 1 | 4584 | yes | persona-driven (S4.1/S4.2); first operator-supplied-context input — new src/ownership.c module kept out of sloth_state_t so an assertion never sits in the observation record; MY_NET_RECON + flood severity scoping + twin-side pinning; Q4 closed |
| 42 (1/n) | 61700b5 | 2026-07-28 | 1 | 4662 | yes | first slice of the SQLite sink: schema v1 + state bucket. Test caught a real inversion — ASSOC_SRC_* is strongest-first so MAX() preserved the weakest evidence; now a non-zero-guarded MIN. CI dep split to 247731c (+40); combined tree scored RISK 90/50 advisory. Event tables, retention, protocol-flow states still to come |
| 42 (1b/n) | 01dc317 | 2026-07-28 | 1 | 4689 | yes | completes the state bucket (28 tables). Counters upsert MAX so ring eviction can't erase brute-force evidence; SMB dialect sticky to SMB1, RDP proto_mask ORs. SNMP community strings deliberately NOT persisted (secret, unlike the username-class identifiers) — guardrail test scans raw file bytes with a positive control |
| 42 (2/n) | ec4a9b7 | 2026-07-28 | 1 | 4743 | yes | event bucket (36 tables), episode-keyed so a persisting alert is 1 row not 3600. Rewrote the MISSION §2 guardrails: substring checks were too coarse (has_pmkid/pw_observed are legitimate flags) — now type-aware via pragma_table_info plus a raw-file byte scan for seeded key material with a positive control |
| 42 (3/n) | 78305dc | 2026-07-28 | 1 | 4783 | yes | tiered retention (1x/3x/12x) + size ceiling that only ever takes observation rows. Ceiling test caught its own inadequate fixture — 4000 small rows stayed under the 1MiB minimum and passed without exercising the guard; now asserts the precondition |
| 42 (4/n) | 376ab88 | 2026-07-28 | 1 | 4809 | yes | closes the #30/#31 evidence hole — karma_ap_t and rogue_radius_ap_t had no jsonl emitter at all, so detector evidence died on exit. Placed in the finding tier, not entity: at 3x it would expire while the CRIT it justifies was still retained, recreating the hole via retention |
| 42 (5/5) | bf10952 | 2026-07-28 | 1 | 4809 | yes | docs/wiki/sqlite-schema.md — the non-obvious parts (assocs strongest-first ordering, evidence in the finding tier, SNMP deviation from the wire format, auto_vacuum caveat). #42 closed after 5 slices |
| 53 | c1f616c | 2026-07-28 | 1 | 4851 | yes | persona G3 — trajectory-shape classifier (pure, hand-built test trajectories), client RSSI ring, Presence column, persisted class. Mutation-checked; found the interior-peak guard is redundant and said so rather than claiming coverage. S3.1 FAIL->PASS |
| 54 | 85ae491 | 2026-07-28 | 1 | 4895 | yes | persona S3.2 — recurring-transit tracker + alert. Coalescing gap is the load-bearing part (a slow drive-by would otherwise read as a circuit); randomisation solved by resolving through the existing seqnum_correlations table rather than a new primitive. All 3 mutations caught. Q3 closed |
| 55 | c4e7467 | 2026-07-28 | 1 | 5460 | yes | persona G4/Q5 — roster extends ownership.c per #52's precedent. Finding is the roster+designation combination; all 4 config permutations tested. Keys on association not probing because per-SSID randomisation is stable while per-probe is not. Both mutations caught |
| 56 | 43a95a6 | 2026-07-28 | 1 | 5489 | yes | persona G5/S5.2 — sessions table + new-since query + report section; the data was already there from #42, only the boundary was missing. Bumped schema to v2 correcting #53 (added a column without a bump, which CREATE TABLE IF NOT EXISTS cannot apply) and moved the version check ahead of schema application |
| 50 | e7ddd7a | 2026-07-28 | 1 | 5499 | yes | --headless + --no-color/NO_COLOR. Measuring the reported symptom found a worse defect: stdin at EOF made select() return instantly so the refresh interval never applied — ~76k redraws/2s burning a core. isatty guard fixes interactive and --no-color runs too |
| B3b | f3682c1 | 2026-07-28 | 1 | 5530 | yes | roadmap item, no issue — unify the two Wi-Fi IE parsers. Extraction behaviour-preserving; 11 tests enter the shared parser the way nl80211 does. Mutation found an untested MFP-required branch. linux_wifi.c is Linux-only so that half is CI-verified only |
| B3 | 8166b6a | 2026-07-28 | 1 | 5636 | yes | retry/FCS tracking. Extracting radiotap from probe.c (not in the test build) gave it coverage for the first time and surfaced a latent bug: the ext-present-bitmap loop never advanced, so those captures yielded no signal or channel at all. 4/4 mutations caught. Also wrote the missing docs/views/channel.md |
| 57 | 989c5d0 | 2026-07-29 | 1 | 5644 | yes | follow-up I filed against my own #46 review: the activate fix closed one route to a no-ifindex datalink, three others stayed silent. Checked the end state (pkt_linktype) rather than the routes, so future ones are covered. WITH_PCAP=0 caught a real gap — capture.c was in TEST_SRCS but not SRCS, so the new call linked in tests and failed in the no-pcap build |
| 58 | 3f1e789 | 2026-07-29 | 1 | 5649 | yes | the one standing -Wformat-truncation warning, a false positive: GCC can't prove buckets[].src is terminated so assumes %s reads on into the rest of buckets[] (1599 B). %.45s bounds it without truncating a real IPv6 addr. Paired with an assertion that full-length v6 addrs survive, verified red at %.20s — a silenced warning with no test is a cosmetic edit |
| 59a | 5432574 | 2026-08-04 | 1 | 5706 | yes | #59 slice 1 of 4 (dispatcher + BTM parse only) — the slice #61 is blocked on, so it ships alone. Spec conflict found: issue calls Disassociation Imminent "bit 3", IEEE Fig 9-924 says B2; implemented per spec with a test pinning both constants. Optional BSS-Term-Duration/Session-URL skip order is the real hazard — wrong length still returns a plausible parse, so each combination is tested and both mutations verified red |
| 60a | 5efb233 | 2026-08-04 | 1 | 5741 | yes | #60 slice 1 of 5 — AKM/pairwise bitmaps in the shared RSN parser, the prerequisite for both this issue's ask-vs-grant delta and #62's transition-mode rule. String AKM was untestable for "PSK and SAE both present": SAE substring-matches FT-SAE, and the 3-entry display cap drops the 4th suite. Zero-on-entry test found a latent bug — beacon_parse_ies zeroed field-by-field, so its documented "fully zeroed" contract broke on any new field; now one memset |
| 60b | 4fbe9f3 | 2026-08-04 | 1 | 5795 | yes | #60 slice 2 of 5 — assoc/reassoc request parse + request table. Reused beacon_parse_ies for the IE walk (B3b) rather than a 4th copy of the loop; discarded its channel/enc outputs as AP-side notions a client frame doesn't carry. Reassoc's 6-byte Current AP Address is the hazard — wrong length shifts every IE but still "parses". Test constant 11:22:.. turned out group-addressed (I/G bit), correctly rejected by the unicast guard |
| 60c | 487b617 | 2026-08-04 | 1 | 5823 | yes | #60 slice 3 of 5 — downgrade delta, but NOT as specified: the issue reads "granted" AKM from the assoc response, which carries no RSNE outside FT/OWE (§9.3.3.7), so that value does not exist for WPA2-PSK or WPA3-SAE. Implemented the observable form instead — a retry asking for less. Both false-positive guards (PSK-arm requirement, strict-< on MFP) mutation-verified; without them an open-BSS roam and every ordinary retry fire |
| 60d | 123a6e8 | 2026-08-04 | 1 | 5852 | yes | #60 slice 4 of 5 — ASSOC_FLOOD, the missing member of the flood set. Dropped the issue's "<= 3 distinct STAs" gate: assoc-flood tooling randomises source MACs, so a real flood is MANY distinct STAs and the gate would suppress the common case while firing on the rare one. Report the count and name the shape instead. Mutation note: the window filter needs both the outer seed check and inner count check removed to fail |
| 60e | a3f5b4d | 2026-08-05 | 1 | 5867 | yes | #60 slice 5a — JSONL wifi_assoc_req + SQLite assoc_reqs + snapshot. DB_SCHEMA_VERSION 2->3 (#59/#61 can ride it). downgrade_flags OR-accumulates: recovery doesn't erase having been moved. Found that reverting the version constant fails NO test — the mechanism is covered but a table added without a bump is undetectable automatically, since fresh files get every table regardless. Added the adjacent check that can be automated: every table needs a retention-tier ageing column |
| 59b | 2cf45d9 | 2026-08-24 | 1 | 6026 | yes | #59 slice 2 of 4 — ALERT_TYPE_BTM_ABUSE. Gated on Disassociation Imminent rather than rate alone: the issue's literal rate rule fires on ordinary 802.11v load balancing, so both counts go in the detail and only the forcing subset crosses the threshold. Two structures per assoc_track precedent — durable pair row for the view, timestamped ring for the window. 5/5 mutants caught, but the candidate-list memset survived round one: overwriting slot 0 hides it until the new list is SHORTER, so the test now shrinks 2 candidates to 1. STA_A in the existing test file is group-addressed (I/G bit) — the same trap #60 slice 2 hit with the same constant |
| 59c | 3aceecf | 2026-08-24 | 1 | 6070 | yes | #59 slice 3 of 4 — SQLite btm_requests + JSONL + [a] view section + --report. No schema bump needed: db_open applies the schema after the version check, so CREATE TABLE IF NOT EXISTS covers a new table on a v3 file — verified by reading db_open rather than trusting slice 5a's note. The view's empty-deauth early return would have hidden the section in exactly the case it exists for (802.11v moves a client with no deauth frame); found by writing the test first. View tests now capture stdout per test_help.c — as smoke tests they'd have passed against a draw rendering nothing. Counters upsert MAX so LRU eviction can't erase evidence behind a live alert. 3/3 mutants caught |
| 59d | 4052fa3 | 2026-08-24 | 1 | 6070 | yes | #59 slice 4 of 4 — docs/wiki/btm-abuse.md + [a] view section + alerts rule row + ROADMAP. Closes #59. The wiki page leads with what a false positive looks like (band steering, a dying radio, a stubborn client) because a behavioural-threshold detector with no such section gets distrusted on its first benign fire. Found two #60 slice-5a drifts while writing: sqlite-schema.md still said v2/40 tables and jsonl-schema.md had no wifi_assoc_req row — fixed rather than documented around, since adding btm_requests beside them would leave the reference more wrong. The schema doc independently confirms slice 3's no-bump call |
| 60f | 10dc6bf | 2026-08-24 | 1 | 6107 | yes | #60 slice 5b of 5 — [w] asked column + delta marker, [k] PHY corroboration, --report, docs. Closes #60. RISK 65/50 advisory, driven by jsonl.c + jsonl-schema.md scoring as an external-contract proxy; the change is one added field, so additive, flagged in the commit rather than waved through. No granted_akm column exists to build: an assoc response carries no RSNE outside FT/OWE (§9.3.3.7). Delta rides as a trailing marker because the row was already 110 cols and prev_akm would read '-' on nearly every row. PHY corroboration enriches existing PNL rows only and leaves probe_count alone — an assoc request is not a probe. Skipped the pnl_clients.phy_confirmed column: a column on an existing table needs v3->v4, which makes every existing database refuse to open, for a display nuance. 3 mutants, 1 survived round one (clean-row check tested for '->' but a flagless marker block still emits a bare delta glyph) |
| 62 | dd736de | 2026-08-25 | 1 | 6151 | yes | #62 — ALERT_TYPE_WPA_DOWNGRADE, four lanes, one alert per (BSSID,kind). RISK 65/50 advisory (jsonl.c + churn; the JSONL change is two added fields). Fired from a rule reading the AP table, not the parser: beacon_parse_ies is pure and reached by both Wi-Fi paths, and the 30s floor needs per-BSSID history. downgrade_flags lives on the record and is filled once per poll so the rule and the [b] view can't drift. MFP-optional narrowed to SAE-only and WPA1 to WPA1-with-RSN — both would otherwise double-count findings wifi_assess already reports. [b] MFP column became posture. 4 mutants, 1 survived round one: the no-pair test had a single AP so removing the OPEN check changed nothing |
| 65 | bf4029e | 2026-08-25 | 1 | 6189 | yes | #65 — PEAP_NO_SERVER_CERT, CVE-2023-52160. RISK 55/50 advisory (new file + churn). Widened eap_track_observe to carry STA and direction — both already derived at the one call site, never passed. Direction is load-bearing: a Certificate travelling STA->AP is the client authenticating and would mask an AP with none. EAP-Failure deliberately not a finding — the client refused, which is correct. No fragment reassembly, so the detail distinguishes 'no ServerHello' (unambiguous) from 'no Certificate observed' (may have crossed a boundary); [z] TLS? column never shows a tick because absence of a finding isn't proof. 4 mutants: one surfaced DEAD CODE rather than a missing test — the entry guard in tls_scan_handshake exactly duplicated the loop's record-header check, removed, real check verified red |
| 63 | fde7bc4 | 2026-08-25 | 1 | 6231 | yes | #63 — ALERT_TYPE_CSA_ABUSE. RISK 65/50 advisory. Storm counts DISTINCT destinations not announcements: an AP repeating one switch per beacon is the standard working, and counting frames would fire on every DFS event. Beacon addr2/addr3 mismatch deliberately not a forgery — equal by construction, so a difference means misparse. [b] PHY cell becomes CSA>N while pending rather than adding a column. 3 mutants: the third surfaced a WRONG COMMENT — the per-BSSID loop guard claimed to prevent per-beacon firing but fire() already dedups by key, so it only saves a ring scan. CSA-and-vanish deferred: needs positive dwell evidence on a hopping sensor |
| 66 | 3510c76 | 2026-08-25 | 1 | 6257 | yes | #66 — HT/VHT/HE/EHT operation IE decode. Durable 6GHz fix: HE Operation 6GHz Info now outranks DS Param, which much 6GHz gear omits; channel_source makes a wrong channel attributable. Three overstate-hazards each pinned: 40MHz needs the width bit AND a secondary offset, VHT width 0 must not overwrite HT, 160 vs 80+80 by centre gap. HE optional-field order (VHT-op, co-host, 6GHz) is the real trap — wrong skip yields a plausible channel, not a failure. CH_WIDTH_UNKNOWN kept distinct from 20: not-decoded is not narrow. Width upgrade-only per record. MCS ceiling deliberately not stored — no reader yet. 4/4 mutants caught |
| 61 | e72ec0f | 2026-08-25 | 1 | 6298 | yes | #61 — RRM_SURVEY_ABUSE. Discriminator is context not rate: fires only when the asker has beaconed SOMETHING but never the SSID asked about. That two-part test is the --hop guard — a BSSID never heard tells us nothing about what it advertises, and collapsing the halves fires on every AP the sweep missed (mutation-verified). Broadcast requests never count toward the threshold; reports counted separately since a reply proves success not attempt. ssid_history exonerates multi-VAP. Footer not column. 3/3 mutants caught |
| 64 | 93d1dd0 | 2026-08-25 | 1 | 6336 | yes | #64 — src/ctrl_frames.c + ALERT_TYPE_RTS_FLOOD. The dispatch had to go ABOVE probe.c 24-byte management guard: every control frame is shorter (CTS 14, RTS 20), so placed below it the module would test green and count nothing in the field. CTS/ACK carry only an RA — attribution comes from the subtype, never the length: a captured CTS includes a 4-byte FCS so a length guess reads 6 bytes past RA and invents a TA from the checksum. First CTS test used bare 10-byte frames, too short for the mutant to bite; now 16-byte realistic ones. Sustain required, not just rate. Illegal NAV (>32767us) is CRIT since no conforming radio emits one. snapshot annotates channel rows, never creates. 3/3 caught after strengthening **Process note**: `git add -A` failed on ignored paths, so the `&&` chain skipped the feature commit while leaving everything staged — the next `git add METRICS.md && git commit` then swept the whole feature into a `chore(metrics)` commit. Already pushed, so not rewritten; rationale recorded in the follow-up `docs:` commit instead. Chain `git add` with explicit paths, not `-A` with exclusions |
| 70 | 2e55128 | 2026-08-25 | 1 | 6371 | yes | #70 — ALERT_TYPE_BLOCKACK_ATTACK (Bl0ck, arXiv 2302.05899). The 12-bit SSN wrap is the whole hazard and was written up on the issue before any code existed: computed as plain subtraction 4090->10 reads as a jump of 4080 rather than an advance of 16, so the rule fires constantly on healthy links — green in CI, useless on air. Modulo 4096, with bar_ssn_forward_distance exposed so the arithmetic is tested directly. Per (receiver, transmitter, TID) — traffic classes have independent sequence spaces. First BAR sets a baseline not a jump, since every connection is joined mid-flight. Multi-TID BARs skipped not misread. Retry burst reported not required — requiring it makes the rule depend on rf_quality sampling the right channel. 4/4 mutants caught |
| 72 | 8d1efd6 | 2026-08-25 | 1 | 6468 | yes | #72 — 802.11 data-to-IP bridge. Arithmetic split into src/dot11_data.c because capture.c is absent from TEST_SRCS and a wrong header length does not fail loudly: it yields a plausible ethertype from payload bytes. The three addends compose (24+6+2+4=36) — treating them as alternatives gets every SINGLE modifier right and the combination wrong, mutation-verified. Encrypted rejected per MISSION §2, as its own status not lumped with malformed, since on most networks it is the normal case. A-MSDU bit read at the computed QoS offset, not a fixed one — it moves when addr4 is present. Truncation test walks every cut point. 4/4 mutants caught. #69 still needs #71 |
| 67 | 5c7d134 | 2026-08-25 | 1 | 6521 | yes | #67 — MLE decode + MLD canonical identity. Not a feature so much as a live mis-count: an MLD radios have independent sequence spaces so seqnum correlation never links them, and one Wi-Fi 7 handset read as three devices. MLE beats seqnum in transit_canonical_mac — assertion over inference; the test makes them disagree and asserts which won. **Found and fixed a regression from #66**: that commit dropped the line setting has_eht while restructuring the tag-255 branch — variable stayed declared and read, compiled clean, suite green, and every AP silently stopped being Wi-Fi 7. Nothing covered the PHY ladder from beacon IEs; now one test per rung. Link Info found via declared Common Info Length not by summing known optionals. Links merge, never replace. 4/4 mutants caught |
| v4 | 03f977d | 2026-08-25 | 1 | 6538 | yes | Batched DB_SCHEMA_VERSION 3->4 — four columns deferred across #60/#62/#65 taken as one break instead of three. All counters upsert MAX/OR, never excluded: bounded LRU tables restart at zero and overwriting erases evidence behind a live alert; downgrade_flags ORs so a reconfigured AP still shows what it once offered. 4/4 mutants caught. **Found a Makefile bug doing it**: TEST_BIN listed no header prerequisites, so a header-only change never rebuilt the test binary — bumping the version and re-running reported on the previous build, making a mutation look like it survived. Every header-only change in this repo history was tested against a stale binary. Fixed; verified both directions |
| 68 | 6c90c75 | 2026-08-25 | 1 | 6582 | yes | #68 — tool-fingerprint mechanism with an EMPTY signature table, by design: a tool fingerprint is an empirical fact about a binary, and invented rows pass every test while matching nothing. PMKID-harvest half works today — a protocol observable, not a vendor quirk; BSSID match load-bearing or every KARMA AP gets someone else evidence. Testing an empty table took two goes: guards are unreachable through the normal entry point, so tests now drive tool_fingerprint_match_table() — the shipped code, not a mirror of its logic. Second mutation found the hits==0 guard provably redundant (strict best-match from zero already rejects it); removed, property re-verified by relaxing the comparison. Also consolidated KARMA_SSID_THRESH, defined twice with the same value |
| 71 | 63988d0 | 2026-08-25 | 1 | 6634 | yes | #71 — HTTP response parsing. The segmentation problem IS the issue and body_complete is the answer: sloth does not reassemble TCP, so the parser reports three states not two, and a consumer must compare bytes only when complete. Different and incomplete are different answers; conflating them turns every large response into a reported hijack. Chunked never complete — the test that earns it is chunked WITH a matching Content-Length, since every other route to incomplete is closed there; that is also the attacker evasion, named in the docs. Pipelining left unpaired, pairing consumes the request, continuation segments rejected. 4 mutants, 1 survived round one: the chunked test reached body_complete=0 by another path so the branch was never under test |
| 69 | d9461d4 | 2026-08-25 | 1 | 6687 | yes | #69 — captive-portal interception. Sentinel table ships FULL (published fixed values) — the opposite of #68 and for the opposite reason. Three independent signals so a portal evading one still fails the others: chunked responses evade the body check and cannot evade DNS. Everything rests on #71 body_complete: a prefix differing from a sentinel is a segment boundary, not an interception, and comparing anyway reports every large page as a hijack. One alert type with kinds, not the seven proposed. DNS_UNEXPECTED is WARN since CDN ranges change. No [C] view — episodic events, [h] decoration instead, flagged. Option 114 and credential-POST named as gaps. 4/4 mutants caught |
| cli | 13b5721 | 2026-08-30 | 1 | 6708 | yes | --version/-V flag + audit of what the stale-test-binary bug could have hidden. Audit clean: across 400 commits only 7 changed a header without touching a tested .c — 6 are SLOTH_VERSION release bumps (version tests already decoupled) and 1 is a29ed7e whose palette constants had no tested consumer then, since tui_palette.c was extracted later. Confirmed the fix rather than assuming: header-only edit now rebuilds, and moving CP_BR_EXTRA_BASE into another bank fails 10 assertions. The new test pins the failure that actually happens — a release forgetting the bump ships a binary claiming to be its predecessor and the updater reports an upgrade forever |
| 73a | 3987e81 | 2026-08-31 | 1 | 6781 | yes | #73 slice 1 of 3 — research corpus + FTS5 ingest. RISK 50/50 advisory (12 new files + churn). Verified both premises rather than trusting: FTS5 compiled in, and three builds byte-identical (sorted path order) so committing research.db is sound. Found the issue proposed [q] for the view — q is the QUIT key, checked before the view switch; free letters are c and f. Parser refuses over half-accepts: a colonless frontmatter line is an error because a typo-d alert_kinds means a doc that never surfaces, and an over-long list is refused not truncated. Guard half-enforced: nonexistent-alert-kind is enforced (whole-token, so ALERT_TYPE_ROGUE cannot pass as a prefix of _ROGUE_RA — both mutation-verified), coverage is warning-only at 11/47 |
| 73b | 0da14f3 | 2026-09-03 | 1 | 7041 | yes | #73 slice 2 (query layer half) — the four ops as a LIBRARY, sloth links it directly rather than spawning a subprocess to query its own file; MCP becomes a transport wrapper. RISK 75/50 advisory. Three bugs the tests found: (1) unicode61 splits on underscores so alert_kinds MATCH ALERT_TYPE_EVIL_TWIN also matches docs naming only _PROXIMITY — now delimiter-wrapped LIKE; (2) for_alert returned sections not documents, so one source appeared once per heading; (3) the report keyed on the display TITLE, which is abbreviated (BLOCKACK_ATK vs ALERT_TYPE_BLOCKACK_ATTACK), silently losing citations — added alert_type_name(). posture_render_md now takes the handle instead of a main.c global: the link failure was the useful kind, since main.c is not in TEST_SRCS and the block was untestable. 5/5 mutants caught |
| 73c | 60cd9eb | 2026-09-03 | 2 | 7329 | yes | #73 slice 2 (MCP transport) — sloth-research-mcp over newline-delimited JSON-RPC, built by `make research-mcp`, not in `all`. RISK 50/50 advisory. Wrote a bounded recursive-descent JSON reader rather than vendoring one or scanning for `"key":` — a scanner reads the tool name out of the user own arguments when they contain `"name"`; there is a test that fails under both a scanner and a prefix matcher. mcp_handle() is a pure fn of (corpus, request, clock) so the wire shapes are testable without spawning a process. Two bugs found: rq_recent was open-ended above (days=1 at any past clock returned the whole corpus — invisible in production until a retrieved date or a clock is wrong, then the weekly brief is silently complete); research-index had been building with -Wcomment since slice 1 because the target is not in `all`. TEST_HDRS extended to research/ — the #67 stale-binary hole was still open there. 11/11 mutants caught, 3 needed new tests (exact key lookup, %g exponent switch above 1e16, and a buffer-size sweep asserting every size yields MCP_OVERFLOW or a complete message) |
| 75a | 366d342 | 2026-09-03 | 1 | 7397 | yes | #75 slice 1 — FragAttacks CVE-2020-26140/-26143 (FRAG_PLAINTEXT) and -26145 (FRAG_BCAST). RISK 50/50 advisory. The gate is the design: per (BSSID, station) and ORDERED, not `the beacon advertised RSN`. Beacon-keyed is unusable — silent on a hopping radio, and association happens in the clear so a per-BSS flag turns every mid-association client into a CRIT against the whole network. Four exemptions each load-bearing (EAPOL/Null/continuation-fragment/non-SNAP). Added dot11_data_addrs(): addr3 is only the BSSID on an IBSS or uplink frame; reading it unconditionally merges every station into one bogus BSS on downlink. NOT building the A-MSDU detector as specified (§4.2) — subframe headers are inside the ciphertext, so the issue premise is unobservable; replacement signal is a duplicate seqnum with a differing A-MSDU bit, slice 3. Corrected the issue CVE mapping (-26146 is non-consecutive PN, broadcast is -26145). Found my own brittle test from 73c: the MCP buffer sweep hard-coded 1200 bytes, so adding a corpus doc broke it — now sweeps to the response natural length. 12/12 mutants caught |
| 75b | c664cb8 | 2026-09-04 | 1 | 7456 | yes | #75 slice 2 — frag_session_t table keyed (BSSID,SA,DA,TID) + FRAG_CACHE (CVE-2020-24586) and FRAG_MIXED (CVE-2020-26147). Neither gated on slice 1's RSN-witnessed state — the defrag buffer exists before decryption. frag_note_association() is its own table, not a reuse of assoc_track.c's (that one merges by evidence strength, wrong rule for "did the cache-clearing event happen"). Dropped -26146 (non-consec PN) despite the wiki page having pre-committed it to this slice: the CCMP PN is one counter shared by every frame on a (key,TID), not per-reassembly, so ordinary interleaved traffic and fragment retries both open gaps that would read as attacks — corrected in docs rather than shipped unusable. Added dot11_data_tid(). research.db regenerated (13/49 -> 15/51 coverage, still warning-only). RISK 15/50 advisory, no schema/JSONL change. 6/6 mutants caught (endpoint OR, strict after-comparison, session close, mixed-protect compare, fragment-zero/continuation branch, session eviction) |
| 74b | 274372c | 2026-09-04 | 1 | 7491 | yes | #74 Detector B, concurrent half — ALERT_TYPE_SAE_PSK_SPLIT. RISK 0/50. This is a GAP in rule_evil_twin, not just new work: its CRIT branch needs a weak enc (OPEN/WEP) beside a strong one and WPA2 is not weak; its WARN branch needs the SAME cipher. A rogue mirroring an SSID at the security level it can actually crack fell between them and produced nothing — the one case the rule most needed. Matched on akm_bits (#60 slice 1) so the whole SAE/PSK families count, not just suites 8 and 2. Explicitly NOT transition mode: each side must offer one family and not the other, so WPA_DOWNGRADE (#62) and this never both fire on one fact — tested both directions. CRIT on differing OUI (removes the benign same-radio explanation) or on a client actually taking the PSK lane via assoc-request AKM. The break matters: fire() dedups on key, so without it three PSK BSSIDs give one row with count 3. NOT covered: the 24h same-BSSID regression — beacon_aps rows are upserted so there is no AKM history, flagged rather than faked. 7/7 mutants caught. Rebased onto a concurrent cloud-routine push of #75 slice 2 |
| 74c | e9001a3 | 2026-09-04 | 1 | 7548 | yes | #74 Detector B, temporal half — ALERT_TYPE_SAE_PSK_REGRESSION over a new ssid_akm_history table. RISK 15/50. NO schema bump needed after all: db.h says CREATE TABLE IF NOT EXISTS runs on every open so a whole new table is safe; only a new COLUMN needs a bump — I had told the issue v5 was required. Neither existing home works: beacon_aps.akm_bits is OR-ed on conflict so transition mode and a regression are identical there; wifi_baseline.c is a session-scoped snapshot with no time dimension. Putting akm_bits IN THE KEY is the design — ON CONFLICT can then only widen a window, never merge two postures. First alert rule to read the DB (only posture.c did); computed at db_tick and cached since a row can only appear at a tick. Query must be latest-not-merely-later (SAE->PSK->OWE, and clearing when SAE returns) and per (SSID,BSSID) INCLUDING inside the MAX subquery (one BSSID many SSIDs is the KARMA shape). Bug found by the tests: the cache survived db_close(), so sloth reported findings from a file it no longer had open and would have attributed old history across a --db swap. 11/11 mutants caught, 3 needed new tests |
| 74d | 10d1052 | 2026-09-04 | 1 | 7596 | yes | #74 fingerprint enrichment -> #68 table. ESP32 Marauder + Pineapple MK7 landed with evidence flagged UNVERIFIED (owner call). RISK 15/50. Key safety property: an `unverified` flag CAPS reported confidence at MED however many fields agree — field count measures how much of the beacon was compared, provenance measures whether the values compared against are right, and a row can pin three things perfectly and still be three guesses. Ceiling not assignment (1-field Pineapple still LOW); a test asserts the flag agrees with the UNVERIFIED evidence prefix. Added require_flags/forbid_flags: half of what identifies a cheap rogue is what its beacon LACKS, and forbidding != omitting. Did NOT pin #74 rate set — supported_rates is on assoc_req_t and nothing fills it on the beacon path, so claiming it inflates the score for a comparison that never happens. Two traps now tested: a flag nothing populates (DEFAULT_HOSTAPD_CAPS), and fp.flags never reaching the matcher from the KARMA rule — zeroing that one assignment left every test green while making both rows inert. AC:0B:FB into kEspressifOuis not a Flipper table (devboard is an ESP32-S2; OUI cannot separate the two). 10/10 mutants caught |
| 75c | c6401eb | 2026-09-04 | 1 | 7674 | yes | #75 slice 3 — ALERT_TYPE_FRAG_AMSDU (CVE-2020-24588). RISK 15/50. The specified detector is impossible: subframe DA/length/LLC are inside the ciphertext. Detect the REPLAY instead — the attacker retransmits a frame the victim already sent with one bit flipped. Keyed on the CCMP PN, not the seqnum as my own triage proposed: seqnum is 12 bits and wraps every 4096 frames (sub-second at real rates) so a seqnum window is both evadable and false-positive-prone; the PN is 48 bits and reuse is itself a protocol violation (12.5.3.4.4), so a repeat is already an anomaly before the flipped bit is considered. Plain retransmissions excluded by construction (same PN AND same bit), not by threshold. Only FragAttacks rule with no key-install gate — the PN is proof of protection. PN byte order is the trap: PN0 PN1 rsvd KeyID PN2..PN5, low octet first and high four AFTER the KeyID, so a big-endian read gives a plausible wrong number; test pins all 48 bits. 7/7 mutants caught |
| 73d | cab6555 | 2026-09-04 | 1 | 7740 | yes | #73 slice 3 — [f] Research view. VIEW_COUNT 33->34. RISK 80/50 ADVISORY (6 new files + 822 lines churn; no contract surface, gate not enforcing). Design: the UNCITED rows are the feature — a view showing only covered kinds answers the easy half of `why should I believe this CRIT`. 18 of 54 kinds cited. Two look-alike states made distinct: `nothing cited` vs `no corpus loaded` (research_open + the --with-research failure reason kept for the header), and a truncated doc list (docs_truncated carried and rendered — 8 shown of 12 reads as complete coverage). One row per KIND not per alert, worst severity not first-seen, ordering stable across polls because the selection is an index. KIND column shows the enum name not the abbreviated title — the same distinction that cost 3 bugs in slice 2. Fixed a -Wformat-truncation by stating the precision (#58 treatment). FOUND: an earlier edit adding ALERT_TYPE_FRAG_AMSDU to the corpus doc had silently not applied — anchor moved under a concurrent cloud-routine push, so the kind was cited by nothing. 9/9 mutants caught; the truncation path needed a synthetic FTS5 corpus since the shipped one has only 6 documents |
| 75d | 5080336 | 2026-09-05 | 1 | 7765 | yes | Daily triage run. Both open issues (#73, #75) already carried a triage comment from a prior run; picked #75's next actionable slice rather than re-triaging. Slice 4 (partial) — ALERT_TYPE_FRAG_AMSDU_EAPOL (CVE-2020-26144), the plaintext half of the aggregation design flaw FRAG_AMSDU detects by PN replay for the encrypted half. Plaintext A-MSDU whose first subframe's LLC/SNAP claims EtherType EAPOL has no benign reading — real EAPOL is never aggregated. Readable only because the frame is unprotected; same key-install gate as FRAG_PLAINTEXT. first_amsdu_subframe_ethertype() is the A-MSDU-aware sibling first_frag_ethertype()'s own comment already pointed to. RISK 15/50 (existing files only). 7/7 mutants caught: EAPOL-exemption polarity, subframe-header-length (14 vs 12), bounds off-by-one, dot11_amsdu_present() gate polarity, branch-selection condition, dropped counter increment, misattributed fire() call. Also found and fixed: origin/main was stale in this container (hadn't fetched a prior session's pushed commits) and local `main` pointed further behind that, landing HEAD in a detached state — fetched, fast-forwarded `main` to origin, and checked it out properly before committing. Also observed: `src/tls_log.c:361` throws a `-Wformat-truncation` warning on this container's gcc 13.3.0 in all five configs including on pre-existing `main` (confirmed via `git stash`) — pre-existing, not introduced here, left unfixed as out of scope for this issue. Remaining on #75: CVE-2020-24587/-26139 need PTK-rotation/handshake-state visibility; the FragAttacks view (slice 5) still open, `c` is the only free keymap letter |
| 75e | ab78f36 | 2026-09-06 | 1 | 7807 | yes | Daily triage run. Both open issues (#73, #75) already carried a triage comment; both still valid, MISSION §2 clean, mid-slice. Picked #75 over #73's remaining content-pass (writing, not code) per MISSION §4's detection-breadth priority. Slice 4 (rest) — ALERT_TYPE_FRAG_MIXKEY (CVE-2020-24587), closing the one gap the #75 triage had flagged as needing state that did not exist. eapol_log.c gained a per-(BSSID, STA) generation counter bumped on an M3 whose ANonce differs from the one last installed (Install=1 is M3's own definition, 802.11-2016 Table 12-8) — ANonce, not "an M3 arrived", because an AP retries M3 verbatim when M4 is lost and counting retries reads ordinary handshake loss as a rekey storm. A fragment session records the counter at open; a completion after the counter has moved spans two keys. Gated on both fragments encrypted (else it's FRAG_MIXED's finding) and a nonzero recorded generation (0 means no evidence, not generation zero). RISK 35/50 advisory (eapol_log.c forensic-output path; 642 lines incl. docs + research.db). Confirmed zero new warnings across all five configs via a stashed diff against origin/main (libpcap-dev was missing in this container; installed it to actually build+verify the default and WITH_SQLITE=0 configs rather than skip them). 9 mutants across eapol_log.c/fragattack.c: 2 survived round one (dropping the start_generation>0 gate — needed a test where the *first-ever* M3 for a pair lands mid-session, not just "no M3 at all"; dropping has_installed so a zero-ANonce first M3 reads as "no change" against the zeroed buffer) — both now have dedicated tests, re-verified caught. Investigated CVE-2020-26139 rather than building it: earlier notes (this run's own prior triage included) called it wired-side-only or vaguely "needs handshake state"; on closer reading the forwarded EAPOL frame would in fact be transmitted wirelessly and is in principle observable, but the exact wire-level shape of "destination" in the attack isn't settled by a one-line advisory, and guessing risks a detector that never fires or fires on ordinary traffic — corrected docs/wiki/fragattacks.md rather than shipping a guess. research.db regenerated (20/56 alert kinds cited, still warning-only). Ledger script still absent in this container (`~/.claude/billing/ledger.py` does not exist) — surfaced per AGENTS.md rather than hand-authored. Remaining on #75: CVE-2020-26139 needs a paper-reading pass, not a coding one; the FragAttacks view (slice 5) still open, `c` still the only free keymap letter |
| 75f | 3e5c48f | 2026-09-07 | 1 | 7842 | yes | Daily triage run. Local `main` had drifted 21 commits behind `origin/main` (stuck at 685838c, HEAD landed detached) — fast-forwarded before touching anything, same class of issue as 75d's stale checkout, this time on local `main` itself rather than origin. Both open issues (#73, #75) already carried a triage comment; both still valid. Picked #75's slice 5 — [c] FragAttacks view — over #73's content pass (writing, not code), same call the last two daily runs made. VIEW_COUNT 34->35. RISK 35/50 advisory. frag_snapshot() mirrors fragattack.c's internal per-BSS table into a new frag_bss_row_t in sloth.h (same decoupling every other WiFi-SIGINT view already has), sorted by last_hit descending. No new SQLite table or evidence blob, per this issue's own earlier reshape call — alert_pcap.c already carries better forensic evidence. protected_frames (the key-install gate's own counter) deliberately excluded from the findings total — summing it in would make an ordinary encrypted BSS with zero attacks read as hundreds of hits. Key `c` verified (not assumed) to be the only free letter in src/main.c's global switch. 5 mutations (findings_total including the gate counter, sort comparison flipped, selection clamp removed, nav upper bound removed, two detail-row fields swapped) all caught. Also found: a second hard-coded `VIEW_COUNT` literal at tests/test_state.c:60 that the enum-bump checklist would have missed if only tests/test_arp.c's copy had been updated — both now say 35. Ledger script still absent in this container — surfaced per AGENTS.md rather than hand-authored |
| 75f | e2a5009 | 2026-09-07 | 1 | 7902 | yes | #75 complete — ALERT_TYPE_FRAG_PN_GAP (CVE-2020-26146) + ALERT_TYPE_FRAG_EAPOL_RELAY (CVE-2020-26139). RISK 15/50. Both were deferred earlier on this issue with written reasons; both reasons were wrong. -26146: the note claimed PNs cannot be consecutive because the counter is shared per key/TID (transmission is serialised per TID, and the PN check is additionally scoped to one sequence number) and that retries get a fresh PN (they do not — a retransmitted MPDU goes out verbatim with the same PN, since it is already encrypted under a nonce derived from it). Shippable because the comparison is dPN == dFN, not +1: a fragment we did not hear advances both equally, and a retry has dFN 0 so it is never compared. -26139: the note chased the mechanism (which address names the destination) instead of the footprint — EAPOL is not peer-to-peer on an infrastructure BSS, so EtherType 0x888E with neither SA nor DA equal to the BSSID is structurally wrong, needing no handshake state at all. Requires ToDS/FromDS (an IBSS would match every EAPOL frame) and unicast. RI_MAX_ALERT_KINDS 8->16: the ingest REFUSED the FragAttacks doc at 9 kinds rather than truncating — slice 1 guard working. Coverage 18/54 -> 22/58. 8/8 mutants caught |
| 76a | 0fda5fd | 2026-09-07 | 1 | 7968 | yes | #76 — ALERT_TYPE_SA_QUERY_FLOOD + ALERT_TYPE_MFP_UNPROTECTED (CVE-2019-16275). RISK 40/50 advisory. Issue premise STALE for the third time in this repo: its grep for subtype-13 dispatch returns 2, not 0, and src/action_snoop.{c,h} has existed since #59. BTM_FLOOD/BTM_ROGUE_STEERING/BTM_COERCED_ROAM are all already BTM_ABUSE, which additionally checks whether a candidate BSSID has EVER been heard beacon (fabricated destination) — a check the issue does not mention. SA_QUERY_FLOOD is the interesting one: it detects the ANSWER not the question. SA Query is MFP refusing a spoofed disassoc, so a storm is the symptom of a disassoc flood aimed at the AP — frames sloth may never hear, while the AP response is on the BSS own channel at ordinary power. Per (BSSID,STA) with a sliding timestamp ring. MFP_UNPROTECTED needs three distinctions: robust is an EXCLUSION list (allow-list goes silently quiet on new categories), capable != required, and beacon_find_mfp returns -1 for never-heard which is NOT 0 (hopping radio would otherwise alert on every untuned network). Group-addressed excluded — BIP uses an MME, not the Protected bit. 7/7 mutants caught |
| 76b | 0afe6b3 | 2026-09-07 | 1 | 7981 | yes | #76 chain detector — shipped as a MARKER on EVIL_TWIN (+btm-steered by AP), not a sixth alert. Owner picked option 1. RISK 0/50. Rationale: every link already fires (BTM_ABUSE, DEAUTH_FLOOD, EVIL_TWIN, EVIL_TWIN_PROXIMITY), so a chain alert is a fifth row for the same seconds of air; KARMA_AP had already settled this shape with +deauth-then-lure. The link is TIGHTER than the issue proposed: not `a steer happened and a twin appeared` (co-occurrence as causation is how a correlator becomes noise) but `the BTM candidate list named THIS BSSID` while carrying Disassociation Imminent — the AP telling a client to associate to the rogue, the whole attack in one field. Escalates WARN->CRIT because a twin with traffic actively pushed at it is not ambiguous; that escalation IS the value of the chain, delivered without a new alert. Two bounds: 300s (the steering table is durable by design so unbounded marks every twin for the session) and imminent-only (a load-balancing controller emits non-imminent all day). 4/4 mutants caught |
| 73e | a720aba | 2026-09-07 | 1 | 8046 | yes | #73 content pass — 24/60 to 59/60 cited. RISK 50/50 advisory (prose churn). 15 documents grouped BY SOURCE not by alert: one MITRE page backs five brute-force detectors, and splitting it would repeat one URL five times for a filing convention. Every doc carries a `what it does not say` section, because for several of these the benign trigger (a stale password in a backup job, VRRP failover, a legacy printer on SMBv1) is the overwhelmingly common case. The accounting change is the real content: ALERT_TYPE_NO_MONITOR_MODE reports sloth own state and can never be cited — alert_technique() already returned `` for it, so coverage.c reads that instead of keeping a second list. Counted as a gap the ratio reads 59/60 forever and a target that cannot be met stops being read; no_basis kinds are now excluded from both halves and render n/a not -. Two tests moved to synthetic one-document FTS5 corpora because PORT_SCAN stopped being uncited — the path is still live (a new detector starts uncited) so it keeps its test. research_coverage_ratio() moved out of the view: a view cannot be tested on what it prints, and a mutant counting no_basis as citable survived until it moved. 4/4 mutants caught |
| 73f | 7deb83b cc54f85 | 2026-09-07 | 1 | 8110 | yes | #73 coverage guard flipped warning-only -> ENFORCING, and the guard itself was fixed first. It had used `alert_kinds MATCH kind` since slice 1 — the same FTS5 tokenizer trap rq_for_alert was fixed for in slice 2, never propagated here: unicode61 splits on underscores and a bare term sequence is a PHRASE, so MATCH ALERT_TYPE_EVIL_TWIN is satisfied by a doc naming only _PROXIMITY. Over-reported coverage for three slices; it reached the right number by luck because every token-prefix kind is also independently cited. Now delimiter-wrapped LIKE with a test asserting BOTH that exact rejects the prefix case AND that MATCH accepts it — without the second half the test records a preference, not a bug. Guard now iterates the enum via alert_type_name()/alert_technique() instead of sscanf-ing include/sloth.h, so a kind cannot escape by formatting. Citable excludes technique==`` (NO_MONITOR_MODE). Verified biting: a synthetic detector with a technique and no doc turns the suite red and is named in the output. AGENTS.md updated in its own commit (+40 risk weighting on agents/) |
| 77 | 4285254 | 2026-09-15 | 1 | 8150 | yes | Daily triage run. Only open issue (#77, "attack-hardware fingerprint layer") had no prior triage comment. Premise stale: #68/#74d already ship tool-naming (vendor-IE hash + capability flags + confidence scoring) wired into the KARMA_AP alert detail, and ESP32 Marauder/Pineapple MK7 are already in the table as UNVERIFIED. Test plan required tests/fixtures/*.pcap — forbidden by agents/AGENTS.md — and the curated per-vendor-stack corpus needs capture hardware this environment doesn't have. Shipped only the buildable-without-hardware slice: WPS IE vendor-string leakage (Manufacturer/Model Name/Model Number/Serial Number, WFA WPS 2.0 §12), spec-defined and hand-testable like the other WPS attributes beacon_snoop.c already walks. No new tool_fingerprint.c rows — inventing an attacker-tool signature without a captured source repeats the exact risk #74 already flagged once. Not persisted to --db (schema-bump cost > a display nuance, same call as #60f's phy_confirmed). RISK 50/50 advisory (jsonl.c + jsonl-schema.md for four additive fields). 3 mutations checked: wrong attribute ID, missing bounds clamp, dropped preserve-guard — all caught, though the preserve-guard test initially passed rsn=NULL on the second record and never exercised the guard at all (the whole update block is skipped when rsn is NULL); fixed to pass a non-NULL rsn with empty vendor strings before re-verifying red |
| 80 | c5d59a5 | 2026-09-17 | 1 | 8248 | yes | ALERT_TYPE_OPEN_SETUP_AP — open (unencrypted) SoftAP named like a device onboarding surface = an unconfigured IoT device exposing an unauthenticated setup surface anyone in RF range can join (provision it, or clone the SSID to harvest the WLAN passphrase). WARN, keyed opensetup:<bssid>, reuses the alerts engine — no new SQLite table, DB_SCHEMA_VERSION stays 4, enum appended last so persisted alert.type values are unchanged. Six-row prefix table (SETUP-, HP-Setup, HP-Print-, DIRECT-, roborock-vacuum-, NETGEAR_EXT) gated behind a carrier/ISP/venue hotspot allowlist + any "guest" token, checked FIRST; encrypted APs and hidden SSIDs never fire; under-flags on purpose. Motivated by the live w9fjc passive run (open Shark vacuum + printer/SETUP- APs in range). Citation is honest: alert_technique() returns "" — this is a third party's victim-side exposure, not an adversary technique; T1557/T1600 would each claim more than was observed (same call BTM_ABUSE made taking T1498 over T1557). Basis cited anyway: NIST SP 1800-36 Vol.A (open-network onboarding risks credential disclosure) + Wi-Fi P2P spec v1.5 3.2.1 (DIRECT- mandates WPA2-PSK, so an open DIRECT- is outside its own spec), research/papers/nist-sp1800-36-onboarding.md. The coverage guard keys on a non-empty technique and would have skipped this detector, so rather than widen the agents/ exemption wording, a new test (test_uncitable_kinds_with_a_real_basis_are_still_cited) names the kind and fails if its doc disappears. Test-first: 7 hand-built beacon_ap cases (open setup fires; 10 public hotspots quiet; 4 ciphers quiet; empty SSID quiet; ordinary open SSID quiet; count-2 across two polls), red before the rule. 8207->8248 assertions. make/WITH_NCURSES=0/embedded/WITH_SQLITE=0 all 0 warnings. research.db regen byte-identical. RISK 20/50 advisory |
| 77 | a56903a | 2026-09-18 | 2 | 8358 | yes | Daily triage run. #77 the only open issue; no new comment from the owner since the previous run, so no re-triage — took the next buildable slice instead. Bullet 2 (beacon interval jitter), observable half only. The receiver-side reading the bullet asks for is unmeasurable here (radiotap TSFT not guaranteed, the radio hops, frames drop — all three move arrival time without the AP moving anything), but the beacon body already carries a better one: IEEE 802.11-2020 §11.1.3 puts TBTTs at whole multiples of the Beacon Interval and §9.3.3.2 order 1 / §9.4.1.10 stamps each beacon with the transmitter's own TSF, so the residual against the nearest multiple is the AP's medium-access deferral on the AP's clock. beacon_parse had never read bytes 24-31 at all. Shipped as measurement only — no threshold, no alert, no TUI row, no tool_fingerprint.c signature: the issue's "±2 TU real / 8-40 TU Marauder" figures have no source verifiable here and agents/AGENTS.md requires detectors to cite theirs; the measurement is spec-grounded, the attribution is not. Same posture as this issue's two prior slices. Additive JSONL only (tbtt_jitter_us / _samples / _resets); not persisted to --db (schema bump invalidates every prior DB file, the #60f phy_confirmed call). Flagged (§4.2): implausible-gap cap anchored to BEACON_AGE_SECS (300 s) rather than an invented number — a delta past the entry's own maximum lifetime is a reset timer, not a long absence; a reset discards accumulated samples rather than blending two clocks, with the reset count published so a BSSID whose TSF keeps restarting isn't silently jitter-free; the accumulator halves at 1024 samples instead of saturating, keeping Σx/n and Σx²/n so the figure stays rolling; stddev published and the mean withheld (the baseline beacon's own unknown deferral offsets every residual — it cancels in stddev, not in the mean); interval carried in TU, not the caller's rounded beacon_ms, because 400 µs/TBTT of rounding error exceeds a whole interval over a few thousand beacons and would corrupt the residual itself. 17 mutants injected across parse/accumulator/derivation/JSONL, all caught — one (dropping the has_tsf guard) initially survived because the "absent TSF" fixture also had a zero interval, so the wrong guard was doing the work; fixture tightened before re-verifying. Converge iteration 1 was red twice (stack smash from an undersized ies[64] in a new test; a wrong expected stddev in the end-to-end fixture — the third timestamp's residual is measured against the second, not the baseline); the fixture was corrected, not the assertion. 8248->8358 assertions. RISK 65/50 advisory (+30 jsonl-schema.md, +20 jsonl.c, +15 churn) — additive fields plus their docs, same shape as the prior two #77 slices. Child session reported one unreproduced WITH_NCURSES=0 warning on its first sweep; parent rebuilt that variant clean four further times (isolated copy of the tree, pre- and post-cherry-pick on main) and could not reproduce it. Remaining on #77 still needs hardware: curated per-vendor stack table, two-radio Pineapple correlation, EAP outer-identity/server-cert CN, verdict scorer |
| 82 | 055357d | 2026-09-21 | 1 | 8366 | yes | Daily triage run. Three open issues: #81 ("git hub wiki") already had a same-day triage comment asking the owner four scoping questions (Copilot's competing branch, source-of-truth, file scope, first-page bootstrap) with no reply yet — left alone. #77 had three prior slices already landed (WPS vendor-string leakage, IE-order hash, TBTT jitter) and its last two comments (2026-09-16, 2026-09-18) already establish every remaining bullet needs either a lab AP or an uncitable threshold — nothing changed since, so no new comment. #82 ("WPS attack surface") was new and unreviewed: verified against the tree and found real gaps — no ALERT_TYPE_WPS_* exists, and Config Methods (0x1008) / Device Password ID (0x1012) were the only two WPS IE attributes beacon_snoop.c's existing WPS parser didn't walk, alongside the vendor strings and state/locked bits it already does. Shipped that slice only: spec-defined TLV parsing (WFA WPS 2.0 §12, same table as #77's vendor-string slice), hand-testable, no hardware. Device Password ID 0x0004 (Push Button) is the payoff — a live, unauthenticated pairing-window signal — surfaced as a `[PBC ACTIVE]` TUI tag and a new HIGH `wifi_assess()` finding distinct from the existing MED "WPS enabled" line. Declined the issue's other asks and said why on the issue: PIN-brute state machine and Pixie-Dust correlation need EAP-WSC decode infra sloth doesn't have (not slice-sized); lockout-cycling and PBC-overlap/flood thresholds and a weak-RNG vendor watchlist have no citable numeric basis reachable from this environment (kb.cert.org is egress-blocked here) — same posture #77 already took on its own uncited thresholds; the issue's pcap-fixture test plan (reaver/wifite2/mdk4 captures) is flatly against agents/AGENTS.md's no-pcap-fixtures rule regardless. Flagged (§4.2): device_pwd_id/config_methods are deliberately NOT sticky like the vendor strings beside them — overwritten every beacon so the live indicator clears once a real session ends, verified by a dedicated test and a passing mutation (making it sticky like its neighbors turns that test red). Not persisted to `--db`, same call as the vendor strings and TBTT jitter. 5 new hand-built tests (parse, absent, short-length-ignored, record-not-sticky, wifi_assess HIGH finding); 3 mutations injected (loosened length guard, sticky merge, wrong Device-Password-ID constant) — all caught, all reverted clean. 8358->8366 assertions. `make`, `WITH_NCURSES=0`, `embedded`, `WITH_SQLITE=0`, `WITH_PCAP=0` all 0 warnings (default build needed `libpcap-dev` installed in-container first — environmental, not a regression, same as #77's note). RISK 50/50 advisory (+30 jsonl-schema.md, +20 jsonl.c) — additive fields plus docs, same shape as every prior WPS slice on #77/#82 |
| 83 | 89e8ff0 | 2026-09-21 | 1 | 8438 | yes | F01 (external CISO/GRC review, tier Immediate) — EAPOL-Key parser heap over-read. parse_eapol_key() rejected len<95 then read the MIC at 81..96 and Key Data Length at 97..98, needing 99; payloads of 95-98 passed the guard and read past the supplied span, off the air via probe_thread -> eapol_observe_dot11. CONFIRMED under ASan before the fix: "heap-buffer-overflow READ of size 16" at the old eapol_log.c:236 MIC memcpy, len 95, through the real observer entry — and 0 ASan/UBSan findings after. Fix works from the span the FRAME declares (4 + body length at bytes 2..3), not the captured length: span>len = TRUNCATED, span<=4 or span<99 = MALFORMED, kdl > span-99 = MALFORMED (replacing the old silent kdl=0, which treated malformed input as "no key data"). size_t + checked subtraction throughout the KDE walk. A reject yields no event, no M1->M2 pairing, no 22000 line, no pcap — only a counter (eapol_reject_count(), under g_mu, zeroed by eapol_clear(), deliberately NOT wired to any view/JSONL/DB so there is no schema change). Export now bounded by the declared span, so capture padding/FCS no longer lands in the hashcat EAPOL field where it breaks MIC verification. 11 new tests, hand-built frames fed from EXACT-SIZED heap allocations (an oversized backing array hides the overread from ASan): two 0..100 sweeps (short capture, and declared-length-matches-short-capture), valid 99-byte header, declared>capture (96, 0xffff), declared body < header (94, 0), kdl 23/0xffff, a PMKID inside the capture but outside the declared body not extracted, kdl shorter than body tolerated, KDE self-overrun yields no PMKID, trailing deadbeef not exported (EAPOL field exactly 198 hex chars), counters ignore unhandled descriptors and reset. Red first: 16 assertions failed, 95-98 reproducing in both sweeps. 8379->8438. Shipped as `refs #83` not `closes`: one regression bullet (ASan through the full capture path in CI) belongs to #95/F13. Docs updated in fd055b8. RISK 35/50 advisory |
| 93 | aecf2ae | 2026-09-21 | 1 | 8485 | yes | F11 (external CISO/GRC review) — data socket framing. data_socket_emit() sent payload and the "\n" as two send() calls, so an EAGAIN on the delimiter left no pending state and the next record concatenated into invalid JSONL; errno was also inspected without first checking for a negative return, so a positive short write could branch on a stale EAGAIN. Both reproduced against the old writer before the fix: consumer saw {"a":1}{"b":2}\n joined, and a record truncated to `lonnex` via the stale-errno path. Fix: per-client queue of COMPLETE records (line + delimiter stored as one unit) with a resume byte offset, so a started record always finishes before the next begins; errno read only on a negative return; short writes and EINTR retry immediately (the old writer treated EINTR as terminal — its own regression test fails pre-fix), EAGAIN keeps bytes queued for the next emit/tick, any other error or 0 closes the client with nothing written after a partial record. Bounds chosen: 512 KiB unsent per client (~2000 typical 250B records, or 32 worst-case 16 KiB ones — sized to absorb the #47 connect baseline burst), growing on demand from 16 KiB, so worst case 16 clients x 512 KiB = 8 MiB; a full queue drops the INCOMING record whole and counts it, never a record already in flight. Stall timeout 30s on the per-poll tick, restarted on every byte the kernel accepts, because a reader that simply stops reading can block a TCP connection forever without ever raising EPIPE; idle clients with nothing queued are never timed out. New consumer-visible record, additive and socket-only (never in -o FILE), emitted once before the next record on a connection that lost data: {"type":"socket_gap","ts","seq","dropped","dropped_total"}, following --out-format so it renders under CEF/syslog too; seq counts records OFFERED to that connection. MISSION §4 read-only surface preserved — no recv/read added. Docs: jsonl-schema.md, log.md, streaming.html, README, examples/consumer (README + sloth-stream.py). Verified by me on main: 0 warnings across make/WITH_NCURSES=0/embedded/WITH_SQLITE=0, and the suite run 8x consecutively at exactly 8485 assertions 0 failed (the new tests use a real socket + drain loop, so flakiness was checked explicitly). 8438->8485. RISK recorded by the child |
| 85 | cda85c5 | 2026-09-22 | 1 | 8533 | yes | Daily triage run. F03 (external CISO/GRC review, tier Immediate): capture-scope restrictions failed open. First slice only. Three changes. (1) Packet filter: the SLL2 allow-list check ran only when ifindex_lookup() returned a non-empty name, and the empty name from a failed if_indextoname() was cached, so every later frame on that index bypassed --iface/--monitor-only. The new capture_frame_in_scope() admits a frame under an active allow-list only if it is SLL2 with a full header and its index resolves to an allowed, non-deselected name. Everything else is dropped before decode. capture_ifname_lookup() caches successes only. (2) Startup refusal: the new pure function capture_scope_verdict() turns an unenforceable scope into stderr plus exit 1 (--monitor-only with no monitor radio, --iface "" leaving the list empty, non-SLL2 datalink). This used to be a warning followed by unrestricted capture. (3) Ordering: capture_start/probe_start are split into open+run. The allow-list is sealed and checked before any JSONL/DB/socket/mDNS sink opens, and before the capture thread exists, with pthread_create as the publication point. Previously the --monitor-only entry landed after the thread was running. Flagged (§4.2): this changes a long-standing default from fail-open to fail-closed. No unrestricted-fallback opt-in flag was added, because omitting --iface/--monitor-only already means unrestricted. No flag was renamed or removed. "Capture unavailable" (no handle) stays a warning, since no stream means nothing out of scope is collected. Tests use hand-built SLL2 headers and a seeded resolver, with no pcap files. The refusal path in main() is covered only through the pure verdict function; the binary was never run (no live interfaces). Parent re-verified on main after cherry-pick: make test 8485->8533, 0 failed; 0 warnings on make/WITH_NCURSES=0/embedded/WITH_SQLITE=0. RISK 45/50 advisory (+30 main.c deletion proxy, +15 churn). Noticed but not touched: a pre-existing g_state memset that follows arg parsing wipes research_status set during parsing. Still open on #85: ifindex reuse/hotplug, scope_not_enforced health event, permission-loss cases |
| 87 | dd6ed49 | 2026-09-22 | 1 | 8635 | yes | F05 (external CISO/GRC review, High when exporting) — export artifacts were created with umask-dependent permissions: --eapol-dir made 0755, files via plain fopen(), so under a common 022 umask a freshly written handshake export was world-readable. A PMKID / 4-way handshake is crackable material. New helper src/secure_file.{c,h}: dirs 0700, files 0600, created by descriptor with O_NOFOLLOW|O_CLOEXEC so umask is irrelevant; export files are opened relative to a dirfd validated once at startup, so re-pointing or renaming the path mid-run cannot redirect the export. Per-artifact create mode decided explicitly: eapol.22000 append with rollback to the previous length on a failed write (hashcat reads the file whole; a full disk must not leave half a line); <bssid>_<sta>.pcap atomic replace via exclusive temp + rename (a failed write keeps the old capture, and a planted symlink is replaced rather than written through); alert pcaps and the Packets-view `w` export exclusive-create with a _2.._99 suffix on same-second name clashes (previously a same-second dump silently overwrote the earlier one — incidental bug fixed); -o JSONL append; --db pre-created 0600 and opened SQLITE_OPEN_NOFOLLOW with existing -wal/-shm/-journal checked first (SQLite copies the main file's mode to the WAL); --report/--report-json validated BEFORE truncation so a refused report leaves the old file intact. Existing paths are VALIDATED, NEVER REPAIRED: refused if not the expected type, owned by another uid, mode & 077 set, multiple hard links, or a symlink — sloth does not chmod an operator-supplied path, it reports a reason and a refused export dir stops startup. Failures are counted and surfaced (stderr + EAPOL view header shows count and latest reason); capture continues and export keeps retrying since a full disk may drain. 22 new tests (56 assertions) red first against the base worktree, incl. permissive-umask, pre-existing permissive file/dir, symlinked export path, symlinked DB, dir-creation failure. 8533->8635. Docs: docs/views/eapol.md gained an "Export handling" section (refusal text + table), plus wiki/README/log updates. Shipped `refs #87`: the opt-in gate for collecting crackable material and its retention policy are a CLI-contract + policy change, deliberately left to the owner. OPERATIONAL NOTE: w9fjc's live /var/log/sloth/sloth.db is root:root 0644 in a 0775 dir and WILL be refused by this policy — chmod 600 the db (+ -wal/-shm) and 700 the dir before deploying. RISK recorded by the child |
| 88 | aebb594 | 2026-09-22 | 1 | 8974 | yes | F06 (external CISO/GRC review) — deauth + probe flood rules did not implement their claimed semantics. Deauth reset only when the gap since the PREVIOUS frame exceeded 5s, i.e. "a chain whose adjacent gaps are each <=5s", so t=0,4,8,12,16 tripped a "5 in 5s" alert although no 5s interval held 5 frames; the flag then persisted until a later reset or record expiry. rule_probe_flood() gated on frame_count>=30 && elapsed>=5s, computed a rate and NEVER compared it to a threshold (comment claimed "by construction the rate is acceptable" — inverted), so 30 probes over 300s fired identically to 30 over 5s. deauth_parse() also decoded body bytes 24..25 as a reason without checking the Protected Frame bit, reporting ciphertext as a decoded reason on PMF networks, and accepted a 24-byte header-only frame as reason 0. Fix: new src/flood_window.{c,h} keeps the last K frame times per key (FLOOD_WIN_CAP 32); flood = the K-th most recent frame is younger than W, a true half-open (t-W, t] window. CLOCK_MONOTONIC drives every duration (window/hold/ageing), wall-clock kept only as evidence/display, with one seam flood_test_set_clock() so tests need no sleeps. Deauth >=5 distinct frames in any 5s window (numbers unchanged — the MEANING now matches what they always claimed); probe >=30 in any 5s window behind an explicit rate predicate (burst_frames/5s >= 6.0/s and burst span <5s), so a long slow history no longer masks a later burst. Flood status decays 10s after the threshold was last met with NO new frame required (hold > window and > the 1s poll, so a short burst cannot be missed); rows age out after 60s idle. Keys are now (BSSID, transmitter, receiver, subtype) — one BSSID no longer overwrites another — plus a separate (BSSID, victim) aggregate for impact. Retry-bit repeats of the previous sequence number do not count toward a flood. Evidence honesty: observed frame counts kept separate from inferred disruption, reason decoded ONLY when unprotected and present. JSONL strictly ADDITIVE: deauth gains reason_valid/retries/protected/truncated/flood_last, probe_client gains flood/flood_last/burst_frames/burst_span_ms. NO DB_SCHEMA_VERSION bump. Three consumer-visible SEMANTIC changes flagged to the owner: (1) deauth record identity was DOCUMENTED bssid+dst but CODED (src,dst) with the BSSID overwritten — now the four keys, so consumers deduping on the old identity will see more distinct rows; (2) DB deauth.reason now binds NULL when undecoded instead of a misleading 0 (column was always nullable, no schema change); (3) DB deauth.flood now means "threshold met at some point in this row's life" (flood||flood_last) while JSONL flood remains "current, decays after 10s" — deliberate, because a tick-sampled DB would otherwise miss a short flood. PCAP FIXTURES REFUSED: the issue's regression list asked for tests/pcaps/flood/, which agents/AGENTS.md forbids outright; every case is a hand-built annotated uint8_t frame per IEEE 802.11-2020 on the injected clock, and tests/ still holds zero .pcap files. Regressions all covered: 5 spaced 4s no-fire, 5 in 2s fire, benign roam, 30/300s no-fire, 30/3s fire, retransmissions, wall-clock jumps, multi-BSSID, broadcast dst, PMF deauth reason not decoded. 8635->8974. Docs: deauth.md, probe.md, alerts.md rows, jsonl-schema.md, karma/twins wording. Verified by me on main: 0 warnings across all four variants, suite run 6x consecutively identical, 0 flaky. RISK 95/50 advisory — entirely surface-driven (+30 jsonl-schema.md as external-contract proxy, +20 src/jsonl.c, +15 three new files, +30 churn 2327 lines), reviewed and accepted |
| 97 | ff96a71 | 2026-09-23 | 4 | 9061 | yes | T12/T13 (external CISO/GRC review, tier Next engineering cycle) — EAPOL handshake pairing had no replay-counter or age check, and the 22000 export mislabelled the message pair. T12: the (BSSID, STA) `pending_t` carried `m1_seen`/`m2_seen` with no Key Replay Counter and no expiry, so the M2 branch set `handshake_complete` whenever `m1_seen` was true — an M1 cached at boot paired with ANY later M2, across associations and rekeys — and a new M1 left the previous attempt's M2, PMKID and buffered frames sitting underneath it. That half-exchange was then handed to `assoc_observe(ASSOC_SRC_EAPOL, ...)`, which called it an association. The record now holds ONE bounded attempt: M1/M2 pair only on EQUAL Key Replay Counters (802.11-2020 §12.7.2, EAPOL offset 9) with the M2 inside `EAPOL_PAIR_WINDOW_S` (10s, chosen against §12.7.6.6's retry budget — hostapd 4x1s — not tuned against traffic); a new M1 (different ANonce or counter) starts a new attempt and discards the M2, PMKID, buffered frames and export flag; a VERBATIM retransmission continues the attempt in flight and re-arms the window, because it is the AP re-offering the same ANonce; message role must agree with frame direction, so an "M3" sent station->AP is recorded and otherwise inert; each attempt exports at most one `WPA*02` line. The three claims are now separate fields because they are separate claims: `handshake_complete` = candidate_message_pair, `handshake_progress` (0-4) = observed_handshake_progress, `assoc_evidence` = association_evidence. Association promotion moved from M2 to M3 — the first point the authenticator commits to this client; M4 does not promote on its own (supplicant-sent, exactly as replayable as the M2 that no longer promotes). None of it is cryptographic verification: sloth does not check the MIC. Per-pair PTK generation state is deliberately NOT reset by an attempt reset — a rekey IS a new M1, and #75 slice 4's FragAttacks mixed-key detector reads that counter across rekeys. T13: the trailing message-pair byte was the literal `02`, which is hashcat's M2+M3 (authorized) category; sloth builds M1's ANonce plus M2's SNonce and MIC, which is `00`. `eapol_message_pair(kind, replaycount_checked)` now derives it, and sets bit 7 ("not replaycount checked") so the byte reports what the tool actually compared. Bit values asserted against https://hashcat.net/wiki/doku.php?id=cracking_wpawpa2, cited in the test, the header and docs/views/eapol.md. Flagged (§4.2): `test_key_exact_99_byte_header_parses` asserted `assoc_count() == 1` after M1+M2 — it PINNED the T12 defect; it now asserts 0, with `test_m3_is_association_evidence` covering the other side (same assertion against corrected behaviour, not a weakened one, and the rewritten comment says so); `eapol_observe_dot11_at()` adds an explicit-clock seam rather than changing the existing signature across 16 call sites, matching `eap_track_observe()`'s idiom, because a time bound cannot be tested by a caller that reads the clock; the window runs from the LATEST M1 including retransmissions (the child's first test asserted the opposite and was corrected, not the code). Child's own self-review caught a live bug it had introduced — `replay_counter_ok` leaked a previous pairing's result onto a later unpaired M2 — fixed per-event and pinned with a test (converge iteration 4). JSONL strictly ADDITIVE: `handshake_progress`, `replay_counter_ok`, `assoc_evidence`; nothing renamed or removed, no DB_SCHEMA_VERSION bump. 18 new tests cover every regression the issue listed: old M1/new M2, mismatched counters, retransmitted M1, M2 without M1, new M1 after an older PMKID, two consecutive attempts, plus window-edge, direction mismatch and retransmitted-M2-exports-once. All frames hand-built `uint8_t` per §12.7.2/§12.7.6 on an injected clock; tests/ still holds zero .pcap files. 8974->9061. Shipped `refs #97`, NOT closed: validating the export against hcxpcapngtool and confirming a known-PSK fixture cracks needs an owned capture and a cracker, and MISSION §2.2 forbids sloth running one — that is a lab step. Verified by me on main after cherry-pick: 0 warnings on make/WITH_NCURSES=0/embedded/WITH_SQLITE=0, `make test` 9061 assertions 0 failed. RISK 80/50 advisory (+30 jsonl-schema.md external-contract proxy, +20 src/eapol_log.c forensic-output path, +30 churn 1071 lines); 453 of the changed lines are tests; no agents/risk_enforce present, reviewed and accepted |
| 91 | 509fa71 | 2026-09-23 | 1 | 9066 | yes | F09 slice 1 of 4 (external CISO/GRC review by Todd Luther) — sensor health was not observable: `chanhop_drive()` called `g_platform.set_channel()` and discarded the return code, so a retune refused for want of CAP_NET_ADMIN, EBUSY, or an unsupported frequency left the interface view's `--hop` scan bar showing the intended channel with no way to distinguish that from a healthy, quiet radio. `chanhop_record_retune()` (`src/wifi_chanhop.c`) is pulled out as pure bookkeeping, no hardware, matching the rest of the scheduler: it always records the requested channel but only advances the confirmed channel on success, and keeps a lifetime `chan_retune_failures` count that a later success does not clear. The scan bar now renders an unconfirmed retune as `[11?]` instead of `[11]`. New `sloth_state_t` fields (`chan_requested`/`chan_confirmed`/`chan_retune_failures`) are read-only bookkeeping, not JSONL — no schema touch. 8 new tests (4 on `chanhop_record_retune` incl. that failures accumulate across an intervening success, 4 on `iface_fmt_scan_bar`'s marker incl. the "never ticked yet" vs "ticked and failed" distinction). 2 mutations injected (record-retune always-confirms, marker predicate hard-wired false) — both caught, reverted. Slices 2-4 (capture-thread exit classification + `pcap_stats()`, `sensor_health` JSONL record + TUI health strip, correlation/PNL-union benchmark at occupancy) tracked separately on #91, not this slice. Verified: `make test` 9061->9066, 0 failed; `make`/`WITH_NCURSES=0`/`embedded`/`WITH_SQLITE=0`/`WITH_PCAP=0` all 0 warnings. RISK 30/50 advisory (`src/main.c` scored as an external-contract proxy by the scorer's file-path heuristic even though no CLI flag or contract changed; the actual touch is a 6-line return-code check inside `chanhop_drive()`) |
| 98 | 54568c8 | 2026-09-23 | 1 | 9147 | yes | T14 — alert escalations never reached consumers: the JSONL/socket `alert` record was emitted once per rule tick with no notion of a run, so a WARN that became CRIT looked identical to the WARN repeating, and a condition that stopped never said so. Additive lifecycle events (`alert.create` / `alert.escalate` / `alert.update` / `alert.resolve`) now bracket an *incident* — one run of one dedup key — carrying a stable `incident_id` (FNV-1a over key + first_detected + a process-wide counter, 16 hex) and an ordered `event_id` of `<incident_id>-NNNN`. The legacy `alert` record is untouched and `count` is kept, now documented as an explicit alias of `evaluations` (rule ticks — it was never a packet or incident count), so no §4.3 schema break. Severity changes always emit in both directions and are never throttled, because severity is the paging signal; detail-only changes are floored at one per 60s (`ALERT_UPDATE_MIN_S`) against an FNV-1a signature of the detail, and the signature is deliberately not advanced while throttled so the next change past the floor still fires with current detail. Rejected a string heuristic for "is this a different victim/pair" — `10.0.0.5` vs `10.0.0.99` is not separable by any normalisation worth trusting; distinguishing pairs is the key's job (#89). Resolve is 300s (`ALERT_RESOLVE_AFTER_S`, matching `JSONL_HEARTBEAT_SECS`) with no rule re-asserting the key, tested on `last_evaluated` not `last_observed`: rules re-derive from retained state every poll, so keying on observation would resolve-and-recreate a standing condition like NO_MONITOR_MODE every five minutes. Durations on CLOCK_MONOTONIC through #88's `flood_window.h` seam; every exported timestamp stays wall clock. Eviction and `alerts_clear()` both close incidents (`reason` = expired / evicted / cleared) rather than dropping them silently. Verified on main: 0 warnings on default, WITH_NCURSES=0, WITH_SQLITE=0, WITH_PCAP=0 and embedded; 9147 assertions, 0 failed (+68 from the new suites in tests/test_alerts.c and tests/test_data_socket.c) |
| 99 | 741e6f1 | 2026-09-23 | 1 | 9147 | yes | Build hygiene, found by me while running the variant gate for #98 and filed as its own issue rather than fixed in passing. `draw_wifi_detail()` and `fmt_akm_mfp()` in `src/views/wifi.c` were defined unconditionally but referenced only from `view_wifi_draw()`'s `WITH_WIFI` branch, so `make WITH_WIFI=0` — the single build where that branch is preprocessed away — emitted two `-Wunused-function` warnings. The helper block now sits inside the same `#ifdef` as its only callers; no behaviour change in any build that has the scan path. The interesting part is not the fix but the cause: `agents/AGENTS.md` stated the warning-clean rule as "`make` produces no warnings", one build out of six, so this variant compiled unwatched for an unknown number of releases. That rule now names all six (`make`, `WITH_NCURSES=0`, `WITH_PCAP=0`, `WITH_WIFI=0`, `WITH_SQLITE=0`, `embedded`) — a deliberate, disclosed change under `agents/`, not a silent one. Verified: all six warning-clean, 9147 assertions, 0 failed |
| 91 | a079de4 | 2026-09-23 | 1 | 9299 | yes | F09 slices 2–3 of 4 (external CISO/GRC review by Todd Luther). "Healthy with no detections" and "not observing" were the same observation, by three separate paths. Both capture workers looped on `pcap_dispatch()`, broke on a negative return and simply returned — the handle stayed open, `capture_is_open()` kept saying yes, and on a hopping radio an empty dwell is normal, so a dead monitor thread was indistinguishable from a quiet channel indefinitely. `pcap_stats()` was never called, so NIC and libpcap buffer drops were invisible. Every bounded table discarded silently at cap, so max occupancy and an empty segment produced the same empty delta. **Slice 2:** each worker classifies its exit (stopped / iface_gone / perm_lost / not_activated / error) and publishes the verdict with libpcap's raw text beside it. `capture_classify_exit()` is pure and sits above the `WITH_PCAP` guard like `capture_activate_failed()`, so it is driven from hand-written return codes with no handle and no radio. A requested stop wins over any error text — `pcap_breakloop()` makes the pending dispatch fail, and reporting that as a fault would cry wolf on every clean shutdown. `PCAP_ERROR` is split by case-insensitive substring on libpcap's wording, which is not a kernel contract, so an unrecognised message degrades to `error` rather than being guessed into a bucket and the raw string still travels in `exit_detail`. Liveness is deliberately a conjunction (run_flag AND no terminal reason): a dead worker leaves the run flag set, because it broke out of its loop rather than being asked to stop. `pcap_stats()` polls once per tick; lifetime totals are accumulated by sloth from per-tick deltas rather than echoed from libpcap's 32-bit counters, so a sample below the previous one reads as a counter reset and a consumer differencing two samples never sees a negative rate. The first sample counts as both total and first delta rather than being discarded as a baseline — the startup window is where an undersized buffer drops hardest. **Slice 3:** the additive `sensor_health` JSONL record is the stream's only singleton — one line per tick about the collector, not one per table row — which it has to be, since the point is to say something when no other record exists. Change-only on the shared snapshot cache, but the signature covers liveness, exit reasons, the channel pair, retune failures, cumulative drops and the eviction total and deliberately NOT recv or any delta: recv climbs every tick on a working sensor, so signing over it would mean a line per second forever and the suppression would be decorative. The interface view's health strip follows the same rule in the other direction — everything after the two liveness words is a fault shown only when non-zero, so a healthy sensor renders exactly `health: cap up  mon up`; a strip printing `drop 0  evict 0` every second would train the operator to stop reading it. **Flagged:** the eviction tally covers alerts, top hosts, PNL clients, per-client PNL SSIDs, DHCP events, 802.1X EAP sessions and the device table (which refuses a new entry rather than evicting — different mechanism, same meaning); it does NOT cover the probe-client, beacon, seqnum, assoc or per-protocol flow rings. The counted set is exactly the one whose modules are in `TEST_SRCS`, so every wired call site has a test driving the real table past its cap — the probe-client ring's eviction site is in `src/capture/probe.c`, compiled only under `WITH_PCAP` and absent from the test build, so instrumenting it would have shipped untested wiring. Both docs state which tables are covered, because a tally that silently omits one reads as "no loss" when it means "not measured". `capture_exit_t` has no EOF member: sloth opens no savefiles, so an offline end-of-capture state is unreachable and a branch for it would be untestable dead code. Exit reason and detail are published under each module's existing mutex rather than as bare volatiles — `exit_detail` is a string and a torn read would hand the operator a truncated error at exactly the moment they need it; the lock is cold. `tests/test_jsonl.c`'s slurp buffer went 4 KiB → 16 KiB because the full-coverage snapshot stream passed 4 KiB once `sensor_health` joined it, and a short read silently turns a `contains()` assertion into a truncation test. Additive only, `DB_SCHEMA_VERSION` unchanged (MISSION §4.3). 28 files, +1811/−12. NOT closed: slice 4 (correlation / PNL-union benchmark at max occupancy) is not done, and the measured adapter/driver/kernel support matrix and comparison against an independent reference receiver both need a radio. Verified by me on main: all six variants 0 warnings, 9304 assertions 0 failed (post-#96) |
| 96 | dd71a06 | 2026-09-23 | 1 | 9304 | yes | External CISO/GRC review (Todd Luther) found our own documentation claiming things the code does not back — the docs/honesty half only; the licensing bullet is the Captain's and the filesystem capacity control is a later code slice. `SECURITY.md` named 1.4.x as current while `SLOTH_VERSION` is 1.8.1, and implied a maintenance model that does not exist; it now names 1.8.1, points at the `#define` as the source of truth, and states the real policy — one branch, newest tag only, no LTS, no backports, no patch SLA. Inventing a support window we cannot keep would be the same defect in the other direction. The embedded IOC list is demo data and the operator had no way to know; a doc-only fix never reaches someone reading the Alerts view, so the disclosure now lives on three surfaces pinned by tests: the alert detail reads `demo IOC`, the help card gained an "Embedded data" section, and the source comments say why no feed ships (a fetch is a network write — MISSION §2). While checking it: four of the six demo domains sit under registrable `.com` labels rather than RFC 2606 reserved TLDs, so "unroutable" was itself overstated and is now "matches nothing in practice". Retention was documented as a 30-day deletion and a hard disk cap and is neither; new `docs/wiki/retention.md` is written from `db_maintain()` — 1x/3x/12x tiers over 14/21/5 tables, age measured from `last_seen` so a still-observed row never expires, hourly and only while sloth runs, a size guard that prunes only observation rows in at most 64 rounds and is allowed to exceed target, measured against the main file's page count with `-wal`/`-shm` uncounted, and two tables in no tier at all. It lists what nothing covers (JSONL, pcap, EAPOL exports, reports, WAL, backups) and states that row deletion is logical, not secure erasure; `--db-max-mb` is retitled a pruning trigger in CLI help, `db.h`, README and the schema wiki to match. Counts swept against source: 35 views (`VIEW_COUNT`), 61 alert rules (one per `ALERT_TYPE_*`), ~48.6k lines across 147 `.c` files — `views-catalog.md` was missing eight views, the marketing pages five, README seven, and `ASSOC_FLOOD` had no row in `docs/views/alerts.md` despite `AGENTS.md` requiring one. Historical files (progress-archive, `RELEASE_*`, the wiki changelog) keep their dated numbers. **Flagged:** `MISSION.md` §1 and §3 edited for factual state, §2 untouched; RISK 90/50 advisory (agents/ instruction files +40, a replaced help-text line in `src/main.c` reads as a deletion +30) — I read every `agents/` and `MISSION.md` hunk myself and they are stale-count and stale-version corrections, not rule changes. Counts were corrected again in `2d4eaaa` after #91 landed, since both branches were cut from the same base and each moved the assertion total. Verified by me on main: all six variants 0 warnings, 9304 assertions 0 failed |
| 84 | 4315676 | 2026-09-24 | 1 | 9351 | yes | F02 slice 1 of 3 (external CISO/GRC review by Todd Luther, tier Immediate) — the headline passivity claim was not backed by the code, and its first bullet had no seam to fix: `dns_lookup()` was the only spelling of name resolution in the tree, so every caller that merely wanted "a name if we already have one" silently became a caller that generates reverse-DNS egress on a cache miss. There was no way to express the difference and therefore no way to test for it. This slice builds the seam and the choke point and deliberately does NOT change what flows through them. `dns_lookup_cached()` answers only from metadata sloth already observed — names injected by the DNS/mDNS/NBNS/DHCP/SNI snoopers via `dns_set_resolved()`, or a reverse resolve that already completed — and queues nothing; critically it claims no `DNS_PENDING` slot, because a refused miss must leave the cache indistinguishable from one never taken or the slot alone would hand the next caller work it did not ask for. `dns_resolve()` is the single choke point: both `enqueue()` call sites now live inside it and `getnameinfo(3)` appears exactly once in `src/`, so gating this one function gates all of sloth's name-resolution egress — which is what slice 2 needs. `dns_lookup()` is retained unchanged as a one-line delegate; nothing renamed or removed, no CLI flag, no JSONL field, no `DB_SCHEMA_VERSION` bump. The observability seam #84's regression section asks for is `dns_resolver_stats()`: cached_lookups / resolve_requests / resolve_enqueued / resolve_suppressed / getnameinfo_calls, with the syscall counter incremented inside `resolve_ip()` rather than at the enqueue site so the number means "work that actually left the process" and not "work that was intended" — a test asserts the two diverge. The test that would have caught the defect is `test_cached_lookup_does_zero_resolver_work`: cold cache, one passive lookup, every resolver counter still zero. **Flagged (§4.2):** the resolver default stays ON — slice 1 is behaviour-preserving by construction, and whether the shipped default flips is open question 1 to the owner, pinned by `test_resolver_enabled_by_default` so the flip arrives as a deliberate edit rather than drift. Every production caller was routed to `dns_resolve()`, including the two the issue names (the UDP/443 QUIC decoder v4+v6 and `top_hosts_update()`), both of which resolve regardless of the UI `dns_enabled` toggle and are arguably a MISSION §2.1 violation on their face; they were NOT silently switched to the passive half, since that is precisely the default-policy question slice 2 must answer, and each site carries a comment saying so. `dns_reset()` deliberately does not clear the counters (cache/queue reset and observability are separate seams; `dns_resolver_stats_reset()` is the explicit clear). `dns_lookup_cached()` mirrors `dns_resolve()` in ageing a RESOLVED entry out at `DNS_TTL_SEC`, so a passively snooped name also expires on a resolver TTL even though nothing was resolved actively — worth deciding in slice 2. The child's self-review found a real defect in our own suite: `test_dhcp_snoop.c` and `test_nbns_snoop.c` call `dns_init()`, so the resolver thread is live during those suites, and `test_discover_no_dns_cache` was taking a cache miss through the active half — `make test` itself could put a genuine PTR query on the wire. Four test files' cache read-backs moved to `dns_lookup_cached()`; no assertion weakened, and that test keeps its original check and gains two counter assertions proving it now stays passive. 16 new tests (dedup, the disabled gate, cache-serving while disabled, the compatibility delegate, IPv6 miss/hit); all addresses are RFC 5737 TEST-NET and no worker drains the queue in `test_dns.c`, so nothing reaches the network. NOT closed: slice 2 (`--strict`/`--observe-only` profile, gating resolver + scan trigger + discovery) is blocked on the owner's two open questions — should strict be the DEFAULT, and which flag name — and slice 3 is the per-interface nl80211 scan-trigger limiter; the issue's over-the-air and host-egress-capture validation needs a lab. Recommendations left for the owner: `dns_init()` starts the worker unconditionally (gating thread start completes the issue's "DNS worker never started" but belongs with the flag), and `dns_resolve()`/`dns_lookup_cached()` return a shared static buffer documented main-thread-only while the capture thread calls the decoder path — a race predating #84 and unchanged here. Verified by me on main after cherry-pick: all six variants (`make`, `WITH_NCURSES=0`, `WITH_PCAP=0`, `WITH_WIFI=0`, `WITH_SQLITE=0`, `embedded`) 0 warnings; `make test` 9304 → 9351 assertions, 0 failed. RISK 15/50 advisory (churn only, 438 lines; selftest 10 passed) |
| 86 | f2bf0b5 | 2026-09-24 | 1 | 9371 | yes | F04 slice 1 of 2 (external CISO/GRC review by Todd Luther) — the socket-lifecycle defects, split from the auth/TLS half left for the owner. `init_unix()` unconditionally `unlink()`'d whatever sat at the path before bind: a regular file, a symlink, or another running instance's live listener were removed identically to a dead socket. `unix_path_removable()` now `lstat()`s (never follows a symlink), refuses anything not `S_ISSOCK` or not owned by our uid, and probes liveness with `connect()` — `ECONNREFUSED` means stale and replaceable, a successful connect means refuse. `set_nonblock()`'s return value was discarded at all three call sites; a failure used to leave a blocking fd reachable under `g_mu`, where `accept()`/`send()` could then stall while the mutex is held. Listener setup now aborts and unlinks the socket file it just bound on failure; a client whose nonblock setup fails is closed before it reaches the client array. `strtol(s, NULL, 10)` accepted a numeric prefix and ignored trailing garbage ("8080x" -> 8080); now checked against the full string via `endptr`. `set_nonblock` is indirected through a new test hook (matching the existing send/accept/clock seams) so both failure paths are reachable without a real EBADF-class fd. 7 new tests; mutation-verified — reverting the removability guard, loosening the port parse, or dropping either nonblock check each turned a test red. The uid-ownership branch has no dedicated test: forging a different uid's file needs privilege this environment doesn't have, left as an honest gap rather than a fake positive control. NOT closed: authenticated TLS/mTLS remote delivery (new crypto dependency, `make embedded` question) and whether loopback-default-plus-documented-trust-boundary closes the review's exposure finding are both the owner's call, per the original triage comment. Verified: all six build variants 0 warnings; `make test` 9338 -> 9371 assertions, 0 failed. `risk_score.sh --selftest` 10/10; RISK 0/50 advisory (churn 298 lines, just under the +15 band) |
| 89 | 09f7e2d | 2026-09-25 | 2 | 9476 | yes | F07 slice 1 of 3 (external CISO/GRC review by Todd Luther) — the evil-twin rules treated three attacker-writable facts as proof of ownership. (1) An 802.11k neighbour report was a *sole suppressor*: `rule_evil_twin`'s same-security branch dropped a pair outright when either AP advertised the other (#51), so an attacker advertising the AP it impersonates erased the finding — the suppression was an off switch handed to the adversary. It is now a confidence deduction that can demote a severity resting only on soft signals and never one backed by a hard signal (attacker-tool OUI, BTM steer), or naming your target becomes a severity lever. (2) A matching vendor OUI was a sole suppressor — three bytes the attacker writes, settable by every rogue-AP tool; a same-OUI pair whose vendor-IE fingerprints contradict each other now fires, while a same-OUI pair with no other signal stays quiet because it carries no positive evidence (a different fact from being trusted, and why a legitimate multi-BSSID deployment is still silent). (3) RSSI no longer names the impostor in the Twins view — it is a fact about distance and antennas, and backwards in the commonest case, where the operator's own AP is the closest radio in the room and the old rule read that as the rogue; attribution now requires an operator designation, a tainted BSSID or an attacker-tool OUI, otherwise the pair is UNATTRIBUTED, canonically ordered and flagged `?`. Severity is split from confidence (5..95%, never 100): severity is how bad the finding is if true, confidence how likely it is true, reported as "suspected impersonation" — collapsing the two axes is what pushed the detector toward suppressing weak candidates to keep noise down, and split, a weak candidate can be shown as weak. Dedup keys become the canonical pair key (rule_id, bssid_lo, bssid_hi, site, security_profile) in byte order so (A,B) and (B,A) are one incident; the old `twin:<ssid>` / `twin-fp:<ssid>` keys collapsed every BSSID pair under one name into one engine record and later evaluations overwrote the earlier pair's detail, so two rogues on one SSID read as one — this reuses #98's dedup/incident machinery and flips that issue's `test_lifecycle_two_twin_pairs_one_ssid_still_merge_see_89`, which #98 deliberately left pinned to the broken count with a comment naming #89. `site` is left EMPTY per the Captain's written constraint on the issue: a site must never be derived from an observed SSID or BSSID, because deriving identity from unauthenticated over-the-air data and then keying a finding on it is the neighbour-report hole wearing a different hat. **Flagged (§4.2):** weighted-evidence scorer chosen over an inventory-free allow/deny model; titles stay `EVIL_TWIN` and the twin_episode fields stay `real_bssid`/`twin_bssid` because both are consumer contracts (JSONL, the `--db` primary key) and renaming them is a §4.3 break — the new meaning rides additive `confidence`/`attributed` fields; `confidence` is omitted from JSONL when zero, since a literal 0 on the ~60 rules that assert what they directly observed would read as "certainly false". Deliberately changed assertions, each of which PINNED the defect (reviewed line by line by me: 18 assertion lines removed against 111 added, none weakened): the two neighbour-report tests asserted no alert at all and now assert the finding survives demoted and less confident; the #98 two-pair test flipped from 1 to 2; `test_twins_lower_rssi_is_real` and `_without_designation_rssi_decides` pinned RSSI-as-ownership and are replaced by tests asserting that inverting the RSSIs does not change the assignment. Additive JSONL only, no `DB_SCHEMA_VERSION` bump. NOT closed: slice 2 is the JSON inventory + flag merge + content hash, slice 3 the impersonator/neighbour/wired-attached UI separation; `attributed` and `confidence` are not persisted to the `--db` twin_episodes table (needs a schema bump), though canonical ordering does fix a real defect in that table's primary key (ssid, real_bssid, twin_bssid), which previously swapped — and so inserted a duplicate row — whenever the pair's two RSSIs crossed. Integration note: this commit was built by a child session on 2026-09-24 and left un-integrated when that run ended; I verified it fresh today rather than trusting the report. Its stated base of 9384 was correct and the `#86` row's `tests_at_ship` of 9371 understates the post-#86 count by 13 — measured 9384 on 91c8a9d myself; this table is append-only so that row stands uncorrected and the discrepancy is recorded here. Verified by me on main after cherry-pick: all six variants (`make`, `WITH_NCURSES=0`, `WITH_PCAP=0`, `WITH_WIFI=0`, `WITH_SQLITE=0`, `embedded`) exit 0 with 0 warnings; `make test` 9384 -> 9476 assertions, 0 failed. RISK 80/50 advisory (+30 docs/wiki/jsonl-schema.md external-contract proxy, +20 src/jsonl.c forensic-output path, +30 churn 1628 lines); `risk_score.sh --selftest` 10 passed 0 failed; no `agents/` or `.github/` changes, no new or deleted files, no root-level files, nothing active on the wire |
| 90 | 00b1bff | 2026-09-25 | 1 | 9475 | yes | F08 slice 1 (external CISO/GRC review by Todd Luther) — KARMA_AP fired CRIT unconditionally once one BSSID beaconed >=3 distinct SSIDs; a long-lived AP that legitimately renamed itself a few times over a session scored identically to an active PineAP lure. Verified the premise against the tree first: `rule_karma_ap` (`src/alerts.c`) called `fire()` with a hardcoded `ALERT_SEV_CRIT` and no gate, PNL overlap/deauth-chain/PMKID were decorative detail notes only, and `karma_deauth_active()` (duplicated in `src/karma_detect.c` as `deauth_active()`, feeding the `[y]` KARMA view's `deauth_chain`/`score` fields and the `--db karma_candidates` table) fired on *any* recent flood anywhere in range with no shared-victim or shared-BSSID correlation — exactly the defect the issue named. Applied the same severity/confidence split #89 gave EVIL_TWIN: a bare SSID-count candidate is now WARN (`fire_conf`, detail ends "- candidate, uncorroborated"); CRIT requires PNL overlap (the actual PineAP Beacon-Response mechanism, not a side effect of it), a shared-victim deauth-then-lure chain, or a verified (capture-backed) tool signature match. New `karma_deauth_lure_victim()` (`src/karma_detect.c`, shared by both call sites, consolidating the duplicate logic the way `KARMA_SSID_THRESH` was consolidated for #68) requires a station deauthed off a *different* BSSID within the 60s window whose PNL already asks for one of the candidate's SSIDs, or who has since associated with the candidate (assoc evidence) — not any unrelated flood elsewhere. `tool_fingerprint_match()`/`_table()` gained a trailing `unverified` out-param so a caller can distinguish a genuinely thin match from one the existing confidence cap was silently capping; an UNVERIFIED tool match is marked with a trailing `?` in the alert detail (`[ESP32 Marauder/med?]`), same convention as #91's `[11?]` unconfirmed-retune marker, and does not escalate severity on its own. PMKID observation stays informational only — it never gated severity even before this change, and the fix makes that explicit in comments: legitimate 802.11r/PMK-caching roaming also produces one, so it is not a third-party-listener tell. **Flagged (§4.2):** confidence weights (`KARMA_W_*`, `src/alerts.h`) are a new judgement call, same shape as #89's `TWIN_W_*`/`TWIN_C_*` — PNL overlap (25) and a verified tool match (30) outweigh a bare threshold hit (20), an UNVERIFIED match (10) and PMKID (10) are informational-weight only. **Assertions deliberately changed, not weakened** (each pinned the defect): `test_karma_three_ssids_fires` asserted CRIT for a bare 3-SSID candidate, now asserts WARN + the uncorroborated note; `test_karma_deauth_then_lure_in_detail` used an unrelated victim on an unrelated BSSID with no PNL/assoc link to the candidate and asserted the chain note fired — split into `test_karma_unrelated_deauth_flood_no_chain` (same setup, now asserts no chain note, no CRIT — the issue's own regression case: "benign SSID-changing AP + unrelated deauth flood must not fire CRIT") and a new `test_karma_deauth_then_lure_shared_victim_in_detail` proving a genuine shared-victim chain (via assoc evidence, decoupled from PNL overlap) still fires and escalates; `test_deauth_chain` in `tests/test_karma.c` (the `[y]` view) similarly flipped to require a real PNL-linked victim, with a new `test_deauth_chain_unrelated_victim_no_credit` covering the negative case. `test_unverified_rows_cannot_report_high` (`tests/test_tool_fingerprint.c`) gained explicit `unverified_out` assertions rather than inferring the property from the confidence cap alone. Mutation-checked by hand (not full-suite mutation testing): forced severity to always-CRIT, forced `karma_deauth_lure_victim` to ignore the shared-victim check, and forced `unverified` to never propagate — all three caught by the new/changed assertions, reverted. Verified on `main` after push: all six variants (`make`, `WITH_NCURSES=0`, `WITH_PCAP=0`, `WITH_WIFI=0`, `WITH_SQLITE=0`, `embedded`) exit 0 with 0 warnings; `make test` 9463 -> 9475 assertions, 0 failed. RISK 15/50 advisory (churn only, 464 lines; no `agents/`, `.github/`, or JSONL-schema-doc path touched — `docs/wiki/jsonl-schema.md` itself was not edited since `confidence` is already a documented generic field from #89); `risk_score.sh --selftest` 10 passed 0 failed. Additive JSONL only (reuses #89's `confidence` field on `fire_conf`), no `DB_SCHEMA_VERSION` bump — `karma_candidates.score` keeps its column, just a corrected input to the same formula. NOT closed: the issue also asks for three explicit states (`PMKID_OBSERVED`/`BEHAVIOR_CONSISTENT_WITH_ACTIVE_COLLECTION`/`CONFIRMED_ATTACK`) as first-class exported fields distinct from severity/confidence — this slice achieves the same operator-facing effect (PMKID never implies attack, severity is gated on real corroboration) through the existing severity+confidence+detail-note model rather than adding new enum fields/JSONL keys, which felt like the right scope for one slice; revisit if the Captain wants the explicit states. `signature_id`/`signature_version`/`validated:bool` as first-class JSONL/socket fields (vs. today's confidence cap + `?` marker + `unverified` out-param) also not done. |
| 99 | 8cd38ef | 2026-09-25 | 1 | 9488 | yes | Follow-up to #99 and precondition for #100. The six-variant warning-clean rule existed in `agents/AGENTS.md`; the enforcement did not. `.github/workflows/ci.yml` built four configurations (default, `WITH_NCURSES=0`, `embedded`, `WITH_SQLITE=0`) — `WITH_PCAP=0` and `WITH_WIFI=0` were checked only by whoever remembered to run them locally, which is exactly how #99 reached main and sat there. Worse, no step failed on a warning: every build asserted only that the code COMPILES, a weaker claim than the contract makes, so even building the missing variants would not have caught #99. Both halves fixed: CI builds all six and exports `EXTRA_CFLAGS=-Werror`. `EXTRA_CFLAGS` is a new Makefile hook appended to `CFLAGS` and `TEST_CFLAGS` after the feature-flag blocks — necessary because `CFLAGS` uses `?=`, so a caller passing `CFLAGS=...-Werror` on the command line would silently drop `-Wall -Wextra -std=c99 -D_DEFAULT_SOURCE` and produce a CI weaker than the one it replaced. Propagation into the `embedded` sub-make was proven, not assumed: probed with `make embedded EXTRA_CFLAGS=-Wbogus-option-xyz` and confirmed the build fails (`cc: error: unrecognized command-line option`). Raised by the Captain's #100 question (a `WITH_TLS=1` opt-in): adding a seventh variant — the only one that cannot compile without a library present — to a matrix already under-enforcing by two is the wrong order of operations, so this lands first and #100 stays captured-not-scheduled. Flagged: touches `agents/AGENTS.md`, the operating contract; the rule itself is unchanged and the added sentence only records that CI now enforces it. Verified by me before push: all six variants build under `-Werror` exit 0 with no diagnostics, `make test` with `-Werror` 9488 assertions 0 failed |
| 89 | 0e00373 | 2026-09-25 | 2 | 10127 | yes | F07 slice 2 of 3 (external CISO/GRC review by Todd Luther) — the approved-inventory trust anchor. Slice 1 removed OUI/RSSI/neighbour-reports as sole suppressors; this gives the rules something legitimate to anchor on instead. New `src/inventory.{c,h}` (545+162 lines) loads a small fixed-shape JSON file — `version`, `site`, `networks[]` of `{ssid, security_profile, bssids[]}` — with a hand-rolled parser rather than a dependency, written against operator-supplied input as DATA: malformed JSON, wrong types, absurd sizes, duplicate BSSIDs, embedded NUL bytes and a missing file each fail cleanly with a named reason, never a crash and never a partial load that silently drops entries. The `--my-ssid`/`--my-bssid` flags (#52) keep working and merge with the file; `--site` added. **`site` is configuration-only by the Captain's explicit decision (2026-09-25)** — `--site` or the file's field and nothing else. It is NOT derived from the uplink association or any observed SSID/BSSID, because a trust anchor built on unauthenticated RF is the exact bug class this issue exists to remove, and because a derived site re-keys on every uplink roam, fragmenting one impersonator into several incidents and making #98's lifecycle open a fresh `alert.create` each time instead of escalating. No `site_source` provenance field — there is no second source. The inventory's **content hash** is the identity, stamped into every alert and export that consulted it so a finding traces to the exact inventory that produced it; the human-readable `version` string is a label only. Pinned explicitly: an alert for an SSID the file does not declare still carries the hash (the inventory was consulted, and said no). With **no inventory configured the behaviour is unchanged** from slice 1 — heuristics stay on, reported as "suspected", confidence split from severity — pinned by test, because an operator who never writes an inventory must not silently lose detection. Reused the shared MAC parser by exporting it from `ownership` rather than duplicating it. Both converge iterations were the child's own test bugs, not code bugs: a `-Wformat-truncation` on the parser's error buffer, and a test using `11:22:33:44:55:66` — which has the group bit set, so `rule_arp_spoof` correctly skips it. 24 files, +2557/−42, 4 new files, all under existing directories; **zero removed or weakened assertions** (639 new). RISK 100/50 advisory (+30 jsonl-schema external-contract proxy, +20 src/jsonl.c forensic path, +20 four new files, +30 churn 2576) — reviewed and accepted; a second scorer run reporting 140 was an artifact of diffing the child's older base against a newer origin/main and counting a `.github/workflows/ci.yml` change it never touched. Verified by me on main after cherry-pick, under the new `-Werror` gate: all six variants exit 0 with zero diagnostics, `make test` 9488 → **10127 assertions, 0 failed** |
| 84 | 4374ff1 | 2026-09-25 | 2 | 10170 | yes | F02 slice 2 of 3 — **strict observation becomes the default**, on the Captain's explicit written decision (2026-09-25). Slice 1 made the passive/active split *expressible* (`dns_lookup_cached()` vs `dns_resolve()`); this makes passive the default posture. `dns_resolver_set_enabled()` defaults OFF; `dns_init()` no longer starts the resolver worker unconditionally; the two callers slice 1 left resolving regardless of the UI toggle — the UDP/443 QUIC decoder (v4+v6) and `top_hosts_update()`, each carrying a comment marking them as slice 2's call — are routed correctly and the comments removed. `--allow-active` opts in and prints exactly one stderr line naming what it enabled, because a passive tool that silently becomes active is the failure this issue exists to prevent. `--strict` is accepted as an explicit no-op that also LOCKS the guarantee for the run (`dns_resolver_lock_strict()`), so a later attempt to enable active behaviour is refused; it changes nothing by itself, and that is the point — operator intent becomes visible in `ps` and in an audit log, which is worth having wherever somebody has to attest to the deployment. `test_resolver_enabled_by_default` was inverted deliberately: it pinned the behaviour the Captain instructed us to change, which is not a weakened assertion. **Flagged, and the most interesting part of the run:** the child saw the assertion count move once (9531 → 9520) on a comment-only change and refused to commit until it understood why — 83 stable runs, then a rebuild under CPU pressure, then 300 more runs after a fix, all stable, never reproduced; base confirmed stable at 9488 across 40 runs. It did not paper over it, and while chasing it found and fixed a real latent bug it had introduced: an unchecked `pthread_create`. Also flagged: four tests had used RFC 5737 TEST-NET addresses that `top_hosts`'s own reserved-address filter silently drops before the code under test, so they were passing vacuously; switched to RFC 2544's non-routable benchmarking range, which survives the filter. RISK 45/50 advisory (+30 is the scorer's conservative proxy for deletions in `src/main.c`, which are additive here — two new flags, nothing renamed or removed). 15 files, +584/−52. Cherry-pick conflicted only in `docs/wiki/log.md` (two same-day changelog entries) — resolved keeping both. Verified by me on main under the `-Werror` gate: all six variants exit 0 zero diagnostics, `make test` 10127 → **10170**, 0 failed |
| 86 | 1d19bc8 | 2026-09-25 | 1 | 10211 | yes | F04 — Option A, the Captain's decision (2026-09-25): **no crypto in sloth**, the transport stays local and remote access is somebody else's job. `--data-socket` already defaulted to `tcp:127.0.0.1:8765` and `f2bf0b5` had already hardened the listener lifecycle, so the remaining exposure was narrow: an operator who explicitly typed a routable address got an unauthenticated listener with nothing in the way. Binding a non-loopback address now **refuses** unless `--data-socket-allow-remote` is passed, and the refusal names the address, the port, and the consequence in plain words ("anyone who can reach that port reads every observation sloth makes") rather than an errno. The guard runs before any socket exists. Loopback and `unix:` behaviour unchanged — no working configuration starts failing. **The child found that a documented claim was false, which is the real value of this slice:** the `unix:` socket was created at the process umask (0775 on the test host), so the "0600, kernel-enforced peer authentication" that this issue's plan — and my own reporting to the Captain — asserted as the recommended deployment **was not true**. It is now, bound 0600 explicitly. That claim had been repeated in three places before anyone checked it. Also: six live docs (including `agents/FACTORY.md` §6.2, which used a Tailscale IP) showed invocations that the new guard would refuse; each fixed, archives left alone. Templates shipped under `examples/stunnel/` (`sensor.conf`, `reader.conf`, README) so the TLS path is deployable rather than merely described — placed under `examples/` because `agents/AGENTS.md` forbids new root-level files. Mutation-verified: four mutations of the guard and parser each turned a test red before being reverted, and the opt-in test proves the flag *permits* the bind without ever opening a routable listener, by binding TEST-NET-1 and reading `EADDRNOTAVAIL` as the kernel's own confirmation that the guard let it through. Reported but deliberately not fixed: a pre-existing inconsistency between this classifier and `src/discovery.c`'s routable/loopback test. RISK 120/50 advisory (checked the gate is in advisory mode before committing). 19 files, +1008/−57. Cherry-pick conflicted in `src/main.c` usage text and flag parsing (three hunks, all additive against #84's flags) and `docs/wiki/log.md`; resolved keeping both sides, then verified by hand that `--help` lists all three new flags and the refusal path fires. Verified by me on main under `-Werror`: all six variants exit 0 zero diagnostics, `make test` 10170 → **10211**, 0 failed |
| 94 | 618f300 | 2026-09-25 | 6 | 10294 | yes | external CISO/GRC review (Todd Luther, 2026-09). Correlation core rewritten to report only what the evidence supports: scored pairs behind an evidence-retention window (--correlate-retain, default 300 s — a pair is reported only while BOTH addresses were heard inside it, counted from now, so nothing is inferred from two trails that were close an hour ago) plus --no-correlate, which switches linking addresses over time off separately from observing the air. Measured, not modelled: the same dense fixture (64 independent radios, monotonic 8-frame trails drawn from only 512 counter positions, each in its own non-overlapping 4 s slot, so every pair is false by construction and what is measured is counter coincidence alone) went 472/2016 = 23.413% accepted on the pre-fix rule to 59/2016 = 2.927%, top score 80%. The test asserts a bound taken FROM that measurement (<4%), so it fails if the rule is loosened; the issue's 3.15% model estimate is encoded nowhere. Top confidence band made structural rather than purely numeric after a coincidence scored 80%. MY_NET_RECON and every other JSONL type id kept: renaming one is a non-additive schema break under MISSION §4.3 and would reach the sloth-ios consumer, and the issue's "rename outputs" was never disambiguated, so human-readable text changed and ids did not. Additive JSONL fields only, no DB_SCHEMA_VERSION bump. RISK 80/50 advisory. Integrated onto main by cherry-pick over #84 slice 2 / #86 with 2 purely additive conflicts (src/main.c flag blocks, docs/wiki/log.md changelog); all six variants + suite re-verified green under -Werror after integration |
| 84 | b9aa686 | 2026-09-25 | 1 | 10508 | yes | F02 slice 3 of 3 — the last code slice, extending slice 2's strict-observation default to the two paths it did not cover, then rewriting the positioning the issue put under review. New `src/observe.{c,h}` is one owner for the run's observation policy: slice 2 kept the strict lock in `src/dns.c` because the resolver was the only active path worth gating, but the nl80211 scan trigger lives in the platform layer and discovery in its own module, and neither can sensibly depend on the DNS module — `dns.c` now keeps only the resolver's enable bit and delegates the lock. **Scan trigger gated and instrumented:** `trigger_scan_async()` fired `NL80211_CMD_TRIGGER_SCAN` from the ordinary poll loop with no policy check; decision, limiter and message construction are split into `linux_wifi_prepare_scan_trigger()`, and the sending path opens no socket when nothing was built. Counting at the builder rather than at `sendto(2)` is the point the issue's Regression section asks for — a request built and then dropped is still a request the code was willing to make. **Limiter made genuinely per-interface:** it was one function-static `time_t` shared across every interface behind a comment claiming per-interface limiting, so the first radio `find_wlan_ifaces()` enumerated consumed the whole budget and the rest starved. Chose the fix over the issue's alternative of documenting it as global: enumeration order is fixed, so "global" does not mean fair, and a dedicated monitor radio alongside an uplink is exactly this tool's deployment. `--strict` also suppresses the mDNS carve-out, enforced inside `discovery_publish()` rather than at the call site. **Flagged (§4.2):** the scan trigger is off by DEFAULT (the Captain's written decision of 2026-09-25), so the WiFi view now reads only the kernel's cached results — `GET_SCAN` is untouched, but on a sensor with no managed-mode uplink the list may be stale until something else on the host scans, documented in `docs/views/wifi.md` rather than worked around. Discovery, by contrast, is suppressed only under the explicit `--strict` lock: the Captain's decision names the resolver and the scan trigger, not discovery, and the carve-out already needs a routable `--data-socket` bind plus `--data-socket-allow-remote`, so it cannot fire on a default run — suppressing it by default would silently break sloth-ios discovery and is the Captain's call, not this slice's. `MISSION.md` §2/§3 and `agents/AGENTS.md` still carry the older "never scans, never modifies kernel state" phrasing; not edited here (charter is §4.3, `agents/` is high-signal) and surfaced on the issue instead. **Both gates confirmed RED before green:** reverting the limiter to the shared static, then stubbing both policy checks, turns 58 assertions red across the new suites and each failure names the behaviour it pins. No assertion weakened, nothing renamed or removed, additive only, no `DB_SCHEMA_VERSION` bump, no JSONL change. RISK 80/50 advisory (`agents/risk_enforce` absent, confirmed before shipping; +30 `src/main.c` deletions is the scorer's conservative external-contract proxy for rewritten usage text — both flags keep their names and semantics, +20 four new files, +30 churn 962); `risk_score.sh --selftest` 10 passed 0 failed. Verified by me before push under `EXTRA_CFLAGS=-Werror`: all six variants (`make`, `WITH_NCURSES=0`, `WITH_PCAP=0`, `WITH_WIFI=0`, `WITH_SQLITE=0`, `embedded`) exit 0 with **0 compiler diagnostics**; `make test` 10294 → **10508 assertions, 0 failed**. NOT closed — the issue's over-the-air validation with an independent receiver per adapter/driver and its host-egress capture both need live radio, which this host never gives sloth; they stay the Captain's to run |
