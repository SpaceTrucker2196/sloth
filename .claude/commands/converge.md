---
description: Run one production order through the factory - issue to green tests to pushed commit
---

Run the sloth converge loop on GitHub issue #$ARGUMENTS as defined in
`agents/converge.md` (the canonical procedure is git-crypt encrypted to
prevent third-party contamination). Read `agents/converge.md` and follow
it exactly. If it appears as ciphertext, run `git-crypt unlock` first.
